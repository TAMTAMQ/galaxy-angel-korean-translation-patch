# Galaxy Angel (PS2) 한글화 진행 상태

갱신: 2026-09-07

## 2026-09-07 전체 자막 영상 PSS / 최종 ISO 해결 및 실기 검증

- `movie/synced`의 실제 영상 30개(`112/113/125`는 원래 없음)에 대응하는 한국어 ASS 30개를 최종 검수했고, 전부 자막 번인 MPEG-2 + PS2 PSS로 생성했다.
- 최종 PSS 산출물은 `movie/subtitled/final/`이며 각 `GADATxxx.PSS`와 대응 빌드 보고서가 있다.
- 이전 실패 원인은 한 가지가 아니라 다음 조건이 겹친 것이었다.
  - 새 MPEG-2를 원본 PSS의 고정 video PES slot에 억지로 stream-fill하면 picture 시작과 PTS/DTS가 같은 PES에 놓이지 않아 게임에서 즉시 스킵될 수 있다.
  - ISO9660 디렉터리 엔트리의 file size를 실제 새 PSS 크기로 갱신하지 않으면 게임이 옛 크기에서 재생을 끝낸다.
  - PSS가 원래 extent보다 커졌는데 LBA를 그대로 두면 뒤 파일을 침범하므로, 실제 새 위치로 extent(LBA)도 함께 갱신해야 한다.
  - 모든 영상을 24fps로 고정하면 `GADAT100`(30fps), `GADAT132`(30000/1001fps)의 프레임 수/타임라인이 깨진다. 반드시 원본 MPEG-2 sequence header의 fps를 유지해야 한다.
- 최종 정상 방식:
  1. `movie/synced/GADATxxx.mkv`에 `movie/subtitles/GADATxxx.ko.ass`를 FFmpeg `ass` 필터로 번인한다.
  2. 640x448 / yuv420p / 4:3 MPEG-2로 인코딩하며 원본 fps를 자동 판독해 그대로 사용한다. 최대 비트레이트는 6Mbps, VBV는 1,835,008 bytes 기준이다.
  3. `tools/galaxy_angel_pssplex_mux.py`로 PSS Plex 호환 형식의 16KiB pack을 만들고, mux rate 7,536,000bps에서 video `0xE0`와 PCM private stream `0xBD`를 배치한다.
  4. GOP timecode + `temporal_reference`로 실제 display frame을 계산해 picture별 PTS/DTS를 생성한다. 24fps뿐 아니라 30fps/29.97fps도 해당 fps의 90kHz clock 간격으로 계산한다.
  5. mux 후 video ES, SShd/SSbd audio stream, 16KiB pack alignment를 다시 파싱해 readback 검증한다.
  6. ISO에서는 새 PSS 실제 sector 수에 맞춰 ISO9660 extent(LBA)와 size의 little/big endian 필드를 모두 갱신한다.
- ISO 배치:
  - `GADAT100~131` 29개는 기존 연속 MOVIE LBA 풀 안에서 새 크기로 다시 촘촘히 배치했다. 총량이 기존 풀보다 작아 다른 게임 데이터는 이동하지 않았다.
  - `GADAT132`는 새 PSS가 148,865,028 bytes로 원래 extent보다 커서 ISO 끝의 새 LBA `2029550`으로 이동했다.
  - ISO Primary Volume Descriptor의 Volume Space Size도 새 최종 sector 수로 갱신했다.
- 최종 검증:
  - PSS 30/30 생성 성공.
  - ISO 내부 30/30 PSS readback SHA-256 일치.
  - ISO9660 extent 중복 없음.
  - 영화 데이터/ISO9660 메타데이터/PVD 크기 필드 외 3,125,118,488 bytes가 이전 한국어 ISO와 byte-for-byte 동일.
  - `build/all_movies_iso_verify.json`: `ok=true`.
  - `GADAT101.PSS` 엔트리가 `GADAT132.PSS`의 LBA/size를 가리키는 `build/Galaxy Angel (Korean)_GADAT132_AS_101_TEST.iso`를 만들어, 사용자가 실제 게임의 101 재생 지점에서 132 재생에 문제가 없음을 확인했다. 이 테스트로 커진 `GADAT132`의 새 LBA/size 및 PSS mux 방식의 실기 호환성을 확정했다.
- 최종 ISO: `build/Galaxy Angel (Korean)_SUBTITLED_MOVIES.iso`
  - 크기: 4,305,385,472 bytes
  - MD5: `e6e1cd4b2f2048842986f28b9a349397`
  - SHA-1: `0f142e90da10f207188b88e137fd4faf1b8d8fe1`
  - SHA-256: `4f5114cc8e94fd3ecfa5bbbec767cab83c7120045c580a148a6760f8dc160d7a`
- 재현/검증 도구:
  - `tools/galaxy_angel_build_all_subtitled_pss.py`
  - `tools/galaxy_angel_pssplex_mux.py`
  - `tools/galaxy_angel_patch_all_movies_iso.py`
  - `tools/galaxy_angel_verify_all_movies_iso.py`
  - `tools/galaxy_angel_alias_movie_test.py`
- 최종 배포 정책은 **본편 한글화 + 전체 영상 한국어 자막을 하나의 v0.1 XDelta에 통합**하는 것으로 확정했다. 사용자는 일본판 원본 ISO에 패치 하나만 적용하면 된다.
  - 지원 원본 ISO SHA-256: `5561d6e0592125eec1d8ef6d5c419b0c14b668868176567f8028be532ce931a3`
  - 최종 통합 ISO SHA-256: `4f5114cc8e94fd3ecfa5bbbec767cab83c7120045c580a148a6760f8dc160d7a`
  - `release/galaxy_angel_ps2_kr_v0.1.xdelta`: 588,645,753 bytes, SHA-256 `85683af4d0d9f956db4ea19fab1b649a0486123e82edd03899c977edd39a3045`
  - `release/Galaxy_Angel_PS2_KO_v0.1.zip`: 571,950,584 bytes, SHA-256 `0752aa31528fee2875968918306321eff769d959a300638f4e4542c7daf3ceda`
  - XDelta를 일본판 원본 ISO에 실제 decode해 최종 통합 ISO의 MD5/SHA-1/SHA-256과 크기가 모두 정확히 일치함을 검증했다.
  - 이전에 시험한 본편/영상 분리 배포안은 폐기했으며, 이후 버전도 특별한 이유가 없으면 영상 PSS를 통합한 단일 XDelta 방식으로 갱신한다.

## 2026-08-27 MINI.DAT 일본어 이미지 전수 한글화

- MINI.DAT의 디코드 가능한 이미지 1,229개(AGI 180개, TAG 1,049개)를 전수
  추출했고, 픽셀 중복을 제거한 776종을 26개 검토 시트로 모두 확인했다.
- 기존 4B 감사의 1차 후보 182종을 다시 검토한 뒤, `qwen3.8-27b`로 776종을
  26배치 전수 재감사했다. 기존 후보 밖의 누락 31종을 추가로 잡아 최종 검토
  후보는 213종이며, 실제 일본어 포함 이미지 185종과 숫자·장식 오인 28종을
  확정했다. 중복 리소스를 포함한 실제 패치 위치는 272곳이다.
- 버튼, 반투명 글자 스프라이트, 막과자 카드, 합성 타이틀, 마작 문패, 볼링
  말풍선·방향계·게이지를 원본 캔버스 크기와 배치에 맞춰 한글로 다시 그렸다.
  새로 확인한 공용 pause의 `いいえ/はい/종료 확인문`, `よーい`, `2P対戦`,
  `親 フォルテ`, `経過時間` 등도 추가 반영했다. `Flying Disk` 영문 영역과
  캐릭터 초상화는 픽셀 단위로 보존했다. 원본 AGI/TAG 팔레트로 재양자화하므로
  이미지 크기·픽셀 포맷은 변하지 않는다.
- 4bpp/8bpp AGI와 PSMT4/PSMT8 TAG를 원래 압축 방식으로 재인코딩했다.
  MINI.DAT 로컬 PIDX 61개와 IDX.DAT 중앙 미러 61개를 동기화했으며, 완성
  리소스를 다시 디코드해 272개 대상의 양자화 후 픽셀 해시가 모두 일치함을
  확인했다. 팔레트 최근접색 계산은 기존 버킷/첫 픽셀 규칙을 그대로 유지한
  NumPy 벡터화로 최적화했고, 기존 구현과 바이트 단위 동일함을 검증했다.
- 전체 빌드 체인에서 기존 시나리오 210블록, 전투 262블록, 일반 한글 이미지
  473종과 런타임 복사본 1,421개, 분기·중앙 인덱스 검증도 함께 통과했다.
  런타임 시나리오 인덱스 stale 복사본은 0건이며 전투 런타임 복사본 795곳도
  최종 ISO에서 다시 검증했다.
- 재현 도구:
  `tools/galaxy_angel_verify_minigame_image_hits.py`,
  `tools/galaxy_angel_localize_minigame_images.py`,
  `tools/galaxy_angel_review_minigame_localization.py`,
  `tools/galaxy_angel_patch_minigame_images.py`.
- 완성 ISO: `build/Galaxy Angel (Korean).iso`
- 크기: 4,156,518,400바이트
- SHA-256: `1e27ddfe40c31a423c38990de999db0bb40326c167b71d0f7a9046a97c396ed8`

## 2026-08-24 IDS/:L 실제 위치 인덱스 재생성

- `GADAT001_DAT_00015800:dialogue:0004` 진입 때 명령 인수 꼬리가 보이고,
  `GADAT001_DAT_00019000:dialogue:0369`에서 `ce(021,0,046,0)`가 대사로
  노출되며 배경·캐릭터가 사라지는 문제를 조사했다.
- 두 JSON의 `Face`/`Voice` 메타데이터와 생성된 명령행은 정상이다. 원인은
  `GADAT001.DAT`의 별도 인덱스 블록(`0x14E000`)이 번역 전 일본어 스크립트의
  `IDSxxxx:{` 및 `:Lxxxx` 바이트 위치를 계속 가리킨 것이었다. 이 때문에 점프가
  `\\Face(...)` 같은 명령 중간에 진입했다.
- 대사와 명령에 공백을 넣어 원문 오프셋을 억지로 유지하던 방식을 폐기했다.
  `galaxy_angel_translation.py`는 번역문을 자연 길이로 만들고 스크립트 종료 뒤만
  원본 해제 크기까지 패딩한다.
- `galaxy_angel_build.py`가 원문과 번역본의 모든 `IDS`/`:L` 대상 위치를 대응해
  고정 관리 영역의 인덱스 값을 다시 만든다. 총 1,135개 대상을 해석했고 위치가
  달라진 910개 값을 갱신했다. 동일 라벨이 한 파일에 여러 번 있는 경우도 출현
  순서로 대응한다.
- 완성 ISO에서 번역 시나리오 210블록이 빌드 입력과 완전 일치하고, 인덱스 해제
  데이터도 재생성 기대값과 완전 일치함을 확인했다. 구조 명령 감사도 231블록,
  문제 0건이다.
- 표기 수정: `アルモ`는 `아르모`, `ローム星系`는 `롬 성계`로 용어집·화자명·
  전체 번역에 반영했다. `로움`, `로옴`, `로음`, `로ーム` 잔존 표기는 0건이다.
- 수정 ISO: `build/Galaxy Angel (Korean).iso`
- SHA-256: `a12f85158aec5b0f54a2d71de69cc867a9ad7b2ab41aaf1ad9a3b02cad19c4f0`

## 2026-08-24 원본 ISO 전체 텍스트 범위 재검사

- 초기 추출은 `GADAT001.DAT` 시나리오 중심이었으며, ISO의 모든 컨테이너와
  전투·미니게임·실행 파일 문자열까지 전수 조사한 상태는 아니었다.
- 원본 ISO의 파일 133개를 모두 열어 원시 CP932 문자열과 모든 유효 Ikusa LZ
  압축 스트림을 검사했다. 일본어를 포함하는 유효 텍스트 압축 블록은 2,061개다.
- 기존 추출분인 GADAT001 시나리오, 선택지, 화자명, GADAT002 `[MESSAGE*]`
  전투 대사 및 동일 런타임 복사본은 해시와 내용으로 중복 제외했다.
- 개발 주석, 설정 주석, MINI 스크립트의 `#` 주석도 번역 대상에서 제외했다.
- 기존 범위 밖 화면용 압축 데이터 번역 후보는 859개이며 컨테이너별 파일은
  `assets/translation/remaining/by_container/`에 있다.
  - `GADAT002.DAT`: 전투 도입·결과·튜토리얼·기체/무기/스테이지 관련 623개
  - `MISC.DAT`: 함내 장소 설명과 기타 화면 문구 133개
  - `GADAT000.DAT`: 세이브/로드와 공용 UI 문구 103개
- 실행 파일 `SLPM_652.54` 및 비압축 영역에서는 화면 문구와 개발 로그가 섞인
  검토 후보 1,070개를 찾았다. 잘못 적용하면 실행 코드 주변 데이터가 손상될 수
  있으므로 `remaining_direct_review.json`에는 기본 `use_translation: false`로
  기록했다.
- 전체 감사 원본은 `analysis/full_text_coverage.json`, 번역용 통합 파일은
  `assets/translation/remaining/remaining_compressed_unique.json`이다.
- 검사 재현 도구:
  `tools/galaxy_angel_audit_text_coverage.py`,
  `tools/galaxy_angel_export_remaining_text.py`.
- 위 수치는 디스크에 CP932 문자열로 저장된 텍스트 범위다. 텍스처·영상 프레임에
  그려진 일본어는 문자열 검사로 검출할 수 없으므로 별도 이미지 시각 검수가 필요하다.

## 2026-08-24 전투 대화 스크립트 추출

- 전투 화면의 `攻撃、はずれちゃった……。／ドンマイ、もう一度！`를
  추적해 `GADAT002.DAT`의 Ikusa LZ 압축 스크립트에서 확인했다.
- `[MESSAGE...]` 섹션의 `#010` 계열은 음성·표정 메타데이터이고,
  `#011`, `#012` 등이 화면에 표시되는 대사행이다. 같은 10단위 그룹의
  대사행을 화면 메시지 하나로 묶어 추출했다.
- 163블록에서 실제 출현 37,805건, 중복 제거 2,302건을 추출했다. 모든 JSON
  원본 행의 바이트 위치를 압축 해제 원본과 대조했으며 불일치는 0건이다.
- 번역용 파일:
  `assets/translation/battle/battle_unique.json`
- 전체 출현·적용 위치:
  `assets/translation/battle/battle_units.json`
- 블록별 파일:
  `assets/translation/battle/segments/GADAT002_DAT_*.json` 163개
- 동일한 전투 스크립트가 `SLGINIT.DAT`에도 런타임 복사본으로 존재한다.
  전체 163블록 모두에서 복사본을 찾았고 총 628개 위치를 JSON의
  `context.runtime_copies`에 기록했다. 적용 단계에서는 GADAT002와 SLGINIT를
  반드시 함께 패치해야 한다.

## 현재 재개 지점

- 기준판은 사용자 확인상 가장 안정적이었던 v30이다.
- v30의 반복 정지는 번역문·줄바꿈·명령문 자체가 아니라, `IDX.DAT` 중앙
  인덱스가 원본 압축 크기와 내부 오프셋을 계속 사용한 것이 근본 원인이었다.
- 정지 상태의 PCSX2 PID 24148 메모리를 직접 읽어 다음을 확인했다.
  - 첫 시나리오 버퍼: EE `0x01092C40`
  - `dialogue:0180` 시작: EE `0x010986A2`
  - 정상 데이터 종료 및 `0xAA` 시작: EE `0x010986CB`
  - 정지한 스크립트 커서: `0x210986DD`
  - v30 압축 스트림을 원본 크기 9,600바이트로 제한하면 런타임과 동일하게
    23,179바이트에서 해제가 끝난다.
- v30을 최소 변경해 `IDX.DAT`의 GADAT001 중앙 레코드 207개를 동기화한
  `build/Galaxy Angel (Korean) v40 central-index-fix.iso`를 생성했다.
- v40 SHA-256:
  `fd96019c31dc570b0a3ead8b2dc7b753dcb2d4d55d0c9944399faf85cdc90b51`
- 정적 검증 결과 GADAT001 로컬 PIDX 318개와 `IDX.DAT` 중앙 인덱스의
  불일치는 0건이다.
- 다음 작업은 v40으로 처음부터 실행하여 첫 선택지, 0177, 0180 이후 진행을
  확인하는 것이다. 현재 실행 중이던 PCSX2 메모리는 이미 9,600바이트 기준으로
  잘려 있으므로 기존 실행을 계속해서는 v40을 검증할 수 없다.
- 이후 일반 `build_patch.ps1` 빌드도 `tools/galaxy_angel_build.py`에서 중앙
  `IDX.DAT` 레코드를 자동 동기화한다.

## 2026-08-24 v40 중앙 IDX.DAT 인덱스 근본 수정

- PCSX2 PID 24148이 `dialogue:0180`에서 멈춘 상태를 직접 조사했다. 첫 시나리오
  원본 버퍼는 EE `0x01092C40`, 0180 번역문은 `0x010986A2`에서 시작하지만,
  `0x010986CB`부터 끝까지 `0xAA`로 남아 있었다. 현재 스크립트 커서
  `0x210986DD`도 이 미해제 구간 안을 가리켰다.
- v30의 첫 시나리오 압축 크기는 9,896바이트지만 게임은 원본 크기
  9,600바이트만 읽고 있었다. v30 압축 스트림을 정확히 9,600바이트에서
  잘라 해제하자 23,179바이트(`0x5A8B`)만 생성됐으며, 런타임의 `0xAA` 시작
  위치와 정확히 일치했다.
- 원인은 각 GADAT 컨테이너의 로컬 PIDX 레코드 외에 `IDX.DAT`에도 파일 ID,
  내부 오프셋, 해제 크기, 압축 크기의 중앙 복사본이 존재하는데, 기존 빌더가
  로컬 PIDX만 갱신한 것이었다. `IDX.DAT`의 GADAT001 파일 ID는 13이며 첫
  레코드에는 원본 튜플 `(0x6000, 24599, 9600)`이 그대로 남아 있었다.
- 빌더가 재배치한 모든 PIDX 레코드를 `IDX.DAT`에도 동기화하도록 수정했다.
  기존 v30에는 207개 중앙 레코드를 최소 변경으로 보정했고, 318개 GADAT001
  로컬 레코드와 중앙 인덱스의 불일치가 0건임을 확인했다.
- 수정 ISO: `build/Galaxy Angel (Korean) v40 central-index-fix.iso`
  SHA-256: `fd96019c31dc570b0a3ead8b2dc7b753dcb2d4d55d0c9944399faf85cdc90b51`
- v40은 v30을 기준으로 `IDX.DAT`만 고친 테스트판이다. 이후 빌드에도 같은
  보정이 자동 적용된다.

## 2026-08-24 v23 고정 관리 영역 + compact 시나리오 배치

- v21은 첫 선택지를 복구했지만 컨테이너가 1,728,512바이트까지 증가했고,
  `dialogue:0176` 뒤 `\SceneChg(3,053,...)`에서 정지했다.
- 번역 시나리오 영역은 변경/미변경 블록을 함께 0x800 정렬로 다시 압축 배치하되,
  `[9999]` 관리 영역이 시작되는 `0x149800`을 고정 경계로 삼았다. 경계를 넘는
  번역 블록 3개만 원본 컨테이너 뒤로 이동했다.
- 결과 컨테이너 크기는 1,454,080바이트로 v21보다 274,432바이트 작고 v15와
  비슷하다. 첫 시나리오 `0x6000`, 관리 블록 `0x149800`, 관리 테이블
  `0x14A800`, ID 인덱스 `0x14E000`은 모두 원본 위치이며, 뒤 세 관리 블록의
  해제 데이터도 원본과 완전히 같다.
- 수정 ISO: `build/Galaxy Angel (Korean) v23.iso`
  SHA-256: `4b878a7f3b8c5bb7f71df5966c4e49ace107aa3ef227fa2cc1a4fa595d2a0a0c`

## 2026-08-24 v21 외부 관리 스크립트 위치 보존

- 원본 선택지를 넣은 v19에서도 창이 뜨지 않고, `<9999->IDS2100`을 같은 길이의
  `w008 = 1`로 대체한 v20에서 첫 선택지가 표시됐다. 선택지 문자열/렌더러가
  아니라 외부 그룹 호출 실패임을 확인했다.
- `<9999->IDS2100`은 `[9999]` 관리 스크립트의 `IDS2100=505`로 이동해
  `w008`~`w013` 선택지 표시 플래그를 켠 뒤 복귀한다. 관리 스크립트는
  `GADAT001.DAT` 내부 원본 오프셋 `0x149800`, ID 인덱스는 `0x14E000`이다.
- 기존 빌더는 번역 시나리오를 넣을 때 PIDX의 318개 압축 블록을 모두 재배치해
  변경하지 않은 관리·인덱스 블록의 내부 위치까지 바꾸고 있었다.
- 빌더를 수정해 원래 슬롯에 들어가는 블록과 변경하지 않은 블록은 제자리에 두고,
  슬롯을 넘는 번역 블록 32개만 컨테이너 끝으로 이동한다. 첫 시나리오 `0x6000`,
  관리 블록 `0x149800`, 관리 테이블 `0x14A800`, ID 인덱스 `0x14E000`이 모두
  원본 내부 위치에 유지되는 것을 완성 ISO에서 확인했다.
- v15의 정상 시작 시나리오와 한글 선택지를 사용한 수정 ISO:
  `build/Galaxy Angel (Korean) v21.iso`
  SHA-256: `aa165a26e2d5a60788a03adecd1bec79ce79a2425d2eb81db68e0254635485fe`

## 2026-08-23 v18 구조 마커 원형 복구

- v17의 첫 선택지 직전 `dialogue:0134` 종료행이 `@@)` 뒤에 240바이트의
  위치 보정 공백을 가진 것을 확인했다. 화면상 대사가 끝나더라도 순차 파서가
  다음 `@(ss)` 선택 블록으로 넘어가지 못할 수 있는 직접적인 구조 변형이다.
- `@@)`, `@(ss`, `@)ss`와 모든 명령행을 원본과 같은 독립 행으로 유지한다.
  분기 위치 보정은 일반 대사행 끝에 소량씩 분산한다. 해당 `그건……` 행의
  보정량은 2바이트이며, 직후 `@@)`부터 첫 선택지 끝까지 위치도 원본과 같다.
- 실제 점프 대상 1,106개의 위치 불일치 0건, 변형된 구조행 0건, 명령 의미
  불일치 0건을 확인했다.
- 수정 ISO: `build/Galaxy Angel (Korean) v18.iso`
  SHA-256: `fa9243c7df8e4ca23e3fb407d00bce0d1e3cdd6f69af250fe4b025c86b837bde`

## 2026-08-23 v17 선택지 직전 명령 원형 복구

- 첫 선택지 직전 `dialogue:0134` 구간을 원본과 대조해, 앵커 길이 보정 때문에
  `\Speaker(001)`와 `\Face(001,0,223,0)`의 숫자 인수 앞에 수백 개의 `0`이
  들어가 있던 문제를 확인했다.
- 숫자값만 같으면 안전하다는 가정을 폐기했다. 길이 보정은 기존 `@@)` 대사 종료
  레코드의 엔진 무시 영역에 분산하고, 모든 명령행은 원본 바이트와 같게 유지한다.
- 전체 시나리오의 명령행 완전 일치 0건 불일치, 선택지 시작·종료 및 분기 앵커
  1,784개 위치 불일치 0건을 확인했다.
- 수정 ISO: `build/Galaxy Angel (Korean) v17.iso`
  SHA-256: `9d57bda392a78827b480be704d9115aacf8079d95bc1633586b6844c093fc79f`

## 2026-08-23 v16 선택지 행 고정 길이 수정

- 선택지 번역을 시나리오 전체에서 전역 치환하던 처리를 제거했다. 이제
  `@(ss`~`@)ss` 블록 내부의 완전히 일치하는 선택지 행만 번역한다.
- 번역된 각 선택지 행은 ASCII 공백으로 원문과 같은 바이트 길이까지 채운다.
  따라서 `\SelStart`, `@)ss`, 후속 분기 명령의 위치가 원본과 동일하다.
- 감사 범위에 `@)ss`도 추가했다. 선택지 시작·종료 및 분기 앵커 1,784개의
  종류·순서·바이트 위치 불일치 0건, 명령 의미 불일치 0건을 확인했다.
- 수정 ISO: `build/Galaxy Angel (Korean) v16.iso`
  SHA-256: `4916fefa7513eae66cfe1757573da7b1a61b0e6e5ce3f26752a72198e87bc2db`

## 2026-08-23 선택지/분기 패딩 근본 수정

- `\SelStart` 처리 함수(`0x0012B9E8`)를 역분석했다. 선택지는 `@(ss` 다음의
  CRLF 구분 행을 최대 5개, 행당 34바이트까지 읽고 `@)ss`에서 메뉴 상태 7로
  전환한다. 선택지 행은 고정 길이 슬롯이 아니다.
- 분기 앵커의 원본 바이트 위치는 유지해야 한다. 이를 제거한 v14에서는 분기가
  `\Face(...)` 중간으로 진입해 `01,0,0,173)`이 대사로 노출됐다.
- 반면 앵커 앞에 CRLF 빈 줄을 삽입한 방식은 순차 파서를 지연·교란했다. v15부터는
  줄을 추가하지 않고 명령의 정수 인수에 의미가 같은 선행 0을 붙인다. 명령이 없는
  구간은 대사 종료 마커 `@@)`와 같은 행의 무시되는 후행 공백으로 채운다.
- 선택지 행은 고정 슬롯이 아니므로 후행 공백 없이 번역문 그대로 저장한다.
- 1,445개 앵커의 종류·순서·바이트 위치 불일치 0건, 명령 의미 불일치 0건,
  추가 CRLF 0건을 확인했다. 파싱 대상 최대 행은 479바이트로 0x200바이트 버퍼
  안이며 선택지 1,345개도 모두 34바이트 이하이다.
- 수정 ISO: `build/Galaxy Angel (Korean) v15.iso`
  SHA-256: `1c0067693ba4ccf46eb3f7f2fdf2a8b69561ff65d7339a87f0952f45f6532f77`

## 2026-08-22 Ar nosurge 방식 Pretendard 래스터라이저 이식

- `D:\trans\Ar-nosurge-korean-translation-patch\tools\build_final_korean_mod.py`의
  최종판 글리프 생성 방식을 확인해 그대로 이식했다.
- Pretendard Bold를 목표 크기의 4배 해상도에서 1.06배 em 크기로 중앙 렌더링하고,
  LANCZOS로 20×20까지 축소한 뒤 24×24 셀 사방에 2픽셀 여백을 둔다.
- 축소 결과의 안티앨리어싱은 게임 폰트의 4bpp 16단계 명암으로 보존한다. 작은 크기에
  직접 렌더링하던 이전 방식의 힌팅 왜곡과 좌우 번짐을 제거했다.
- 저장 상태에 새 글리프 13,824바이트를 EE RAM에 다시 주입한 뒤 실제 대사창에서
  검증했다. 캡처: `runtime_pretendard_arnosurge.png`.
- 검증 ISO SHA-256:
  `6f2baadb5655552c2c9898ffb6e263720dbf7bbeacf37b95cd95f7ffed5ca2d1`
- 최초 이식본에서 글자가 좌우로 흩어진 원인은 4bpp 니블 순서 오류였다. 원본 `人`
  글리프를 양쪽 순서로 복호화해 비교한 결과, 이 게임은 왼쪽 픽셀을 낮은 니블에,
  오른쪽 픽셀을 높은 니블에 저장한다. 생성기의 패킹을 이 순서로 수정했다.
- 니블 수정 후 실제 대사창 검증 캡처: `runtime_pretendard_nibble_fixed.png`.

## 2026-08-22 첫 번역 정지 문제 해결

- 타이틀의 `처음부터` 선택 뒤 검은 화면에서 멈춘 직접 원인은 번역 후
  `GADAT001_DAT_00006000` 해제 크기가 원본 24,599바이트보다 커진 것이었다.
- 첫 번역을 원본과 같은 24,599바이트로 조정한 ISO에서 오프닝 재생, 한국어 첫 대사
  두 페이지 표시, 다음 일본어 대사 블록 진행까지 emucap/PCSX2로 검증했다.
- 증거 캡처: `runtime_exact_raw_size_after.png`, `runtime_next_dialogue_ok.png`,
  `runtime_second_unit_ok.png`.
- `galaxy_angel_translation.py`와 `galaxy_angel_build.py`가 번역된 해제 블록의 크기가
  원본과 다르면 빌드를 중단하도록 보강했다. 압축기 자체 왕복 검증도 계속 적용된다.
- 검증 ISO SHA-256:
  `26554f9cbbcb9825d1f04ba9fd3104cf84f4cf7554f792b71ea86f2c77b36d09`

## 2026-08-22 한글 글꼴 선명도 개선

- 맑은 고딕 Bold 크기를 20에서 21로 올리고, 1픽셀·알파 160의 중간 농도 외곽선을
  추가해 원본 일본어 글리프와 비슷한 획 점유율을 확보했다.
- 내부 획은 최대 농도, 가장자리는 4bpp 그레이스케일 안티앨리어싱을 유지해 작은
  24×24 셀에서도 끊겨 보이지 않도록 했다.
- 실제 대사창 검증 캡처: `runtime_crisp_font_dialogue.png`.
- 개선 ISO SHA-256:
  `da1974248445105c066bca26e010c88df1bafaaef79219a01dc638b5afee737a`

## 2026-08-22 Galmuri 픽셀 글꼴로 재교체

- `font.emulog.app`의 PS2용 Galmuri14 20×22 사례와 Galmuri11 Bold 목록을 참고했다.
- 공식 `quiple/galmuri` 저장소를 `vendor/galmuri/`에 받아 OFL-1.1 원본을 사용한다.
- 최종 선택은 `Galmuri11-Bold.ttf`, 20px, 외곽선 0이다. 맑은 고딕에 덧씌웠던
  반투명 외곽선을 제거해 좌우 번짐을 없애고, 폰트 자체의 굵은 픽셀 획을 사용한다.
- 실제 대사창 검증 캡처: `runtime_galmuri11_bold_dialogue.png`.
- ISO SHA-256:
  `9daa88251f20e6ce3baebb0329d1a3a01f0b2dda3b31b3f1ee6073cb314cb1d6`

## 2026-08-22 번역 직전 단계 완료

- 본문 글꼴은 ELF 가상주소 `0x0033D710`의 24×24, 4bpp, 글리프당 288바이트
  내장 테이블로 확정했다. `0x0014D530`이 Shift-JIS를 압축 JIS 인덱스로 변환한다.
- 압축 인덱스 5,000~7,044의 후반부 글리프 2,045개를 한국어 전용으로 동적 배정한다.
  `tools/galaxy_angel_font.py`가 번역 JSON에서 CP932로 표현 불가능한 문자를 수집하고,
  맑은 고딕 Bold 글리프와 `font_map.json`, 패치 ELF를 자동 생성한다.
- `tools/galaxy_angel_translation.py apply --encoding-map`이 한국어 문자를 엔진용
  2바이트 코드로 변환한다.
- `tools/galaxy_angel_build.py`가 ISO9660 파일 위치를 보존한 채 ELF와 변경된 Ikusa LZSS
  시나리오 블록만 교체하고 PIDX0의 원본/압축 크기를 갱신한다.
- 미번역 빌드는 원본 ISO와 SHA-256이 완전히 동일했다.
  `5561d6e0592125eec1d8ef6d5c419b0c14b668868176567f8028be532ce931a3`
- 임시 한국어 대사 1블록(42개 고유 한글)을 실제 ISO로 빌드해 PCSX2/emucap에서
  정상 부팅, 시나리오 재압축/재삽입, 실제 대사창 한글 표시를 검증했다.
  증거 캡처: `build/hangul_iso_poc_finalfont.png`.
- 임시 번역은 검증 후 JSON에서 제거했다. 이제 사용자는
  `assets/translation/segments/*.json`의 `translation`, `state`, `use_translation`만
  채우고 `build_patch.ps1`을 실행하면 된다.

## 2026-08-22 추가 추적: 실제 텍스트 객체 경계

- 시나리오 런타임 슬롯(`0x00A6FC30`)은 `0x0012DBC0`에서 Shift-JIS 바이트 단위로 채워지고,
  미러 슬롯(`0x00A72A1C` 계열)로 복사된다.
- 미러 슬롯의 첫 소비는 범용 `strlen`(`0x002C0654`)이며, 호출자 `0x00132C08` 루프가
  최대 17개 슬롯을 검사한 뒤 `0x0014BD48`을 호출한다.
- `0x0014BD48`은 전달된 실제 문자열을 새 힙 버퍼로 복사하고 텍스트 객체 `+0x0C`에
  포인터를 저장한다. 실측 예: 객체 `0x01004980`, 복사 문자열 `0x01004C30`.
- 복사 문자열의 다음 소비자는 `0x001488xx` 객체 갱신 루틴이다. 이 루틴은 `+0x60`의
  문자 진행 위치와 `+0x78` 상태를 갱신하고 `0x0014CBD0` 계열 렌더 요청을 호출한다.
- 따라서 이후 글리프 추적은 시나리오/미러 버퍼가 아니라 텍스트 객체 `+0x0C`가 가리키는
  힙 문자열과 `0x001488xx` 이후 경로를 기준으로 한다.
- 현재 PCSX2 디버거가 과거 프레임 경계 `0x00152238`에서 중단점 목록과 무관하게 반복
  정지하는 현상이 있어, 저장 상태 재적재와 ELF 정적 디스어셈블을 병행해 우회 중이다.

## 원본 검증

- ISO: `D:\game\pcsx2-v2.6.3-windows-x64-Qt\roms\Galaxy Angel (Japan).iso`
- SHA-256: `5561d6e0592125eec1d8ef6d5c419b0c14b668868176567f8028be532ce931a3`
- 인수인계 문서의 해시와 일치함.
- 추출한 `GADAT000.DAT` SHA-256:
  `97350e38b88b6963a287153a91b643847848cc261c8bc6cce33cc171f31f692`

## 폰트 조사 결과

기존 인수인계의 “`GADAT000.DAT` 안 `font.tbl`/`font16.tbl`이 폰트”라는 설명은
부분적으로만 맞다. 두 파일은 실제 글리프 데이터가 아니라 확장 글자 설정이다.

- `font.tbl` (`GADAT000.DAT` 블록 `0x14000`)
  - `Folder=dat\gadat032.pak+`
  - `pjfont00.t32`, `pjfont01.t32`, `gigaiji3.tan`, `gigaiji4.t32`, `gigaiji5.t32`
- `font16.tbl` (`GADAT000.DAT` 블록 `0x14800`)
  - `Folder=dat\gadat042.pak+`
  - `cjfont00.t32`, `cjfont01.t32`
- `[FONT_EX]` 주석은 “사용 외자의 선언”이며 1~31번 확장 글자를 지정한다고 설명한다.
- 디스크에는 `GADAT042.DAT`가 없고 `GADAT032.DAT`만 있다. `font16.tbl`은 다른
  빌드/모드용 설정일 가능성이 있으나 아직 확정하지 않았다.

`GADAT032.DAT`에서 이름이 `pjfont00.tex` 등으로 등록된 멤버를 찾아 해제했다.

| 멤버 | 컨테이너 오프셋 | 해제 크기 | TEX 크기 필드 | 판정 |
|---|---:|---:|---:|---|
| `pjfont00.tex` | `0x1275000` | 1,664 B | 20×20 | 확장 아이콘 |
| `pjfont01.tex` | `0x1275800` | 6,464 B | 40×40 | 확장 아이콘 |
| `pjfont02.tex` | `0x1276000` | 848 B | 14×14 | 확장 아이콘 |

따라서 본문 일본어 폰트의 실제 공급원은 아직 미확정이다. 현재 유력한 가설은
실행 파일에 링크된 `CGMan::CreateTextObj` 계층 또는 런타임 생성/시스템 폰트다.
ELF에는 `m_hFont`, `m_nFontH`, `CGMan::CreateTextObj`, `CGMan::ResetText` 문자열이 있다.

## 추가한 조사 도구

- `tools/galaxy_angel_font_probe.py`
  - `GADAT000.DAT`의 0x800 정렬 Ikusa LZSS 블록을 전부 해제하고 지문을 기록한다.
- `tools/mips_string_xrefs.py`
  - MIPS ELF에서 ASCII 문자열의 단순 `lui` + `addiu/ori` 참조를 찾는다.

두 스크립트는 `py_compile`을 통과했다.

## emucap 상태

- `.mcp.json`에 `emucap-control`, `emucap-track` 등록이 존재한다.
- MCP 서버, PCSX2 브리지, 고정 PCSX2 포크 실행 파일도 모두 존재한다.
- Codex 네이티브 도구 목록에는 노출되지 않았지만 `tools/emucap_stdio_run.py`로
  MCP stdio 서버를 직접 제어할 수 있다. 지속형 `--interactive` 모드를 사용해야 한다.
  단발 모드는 클라이언트 종료와 함께 제어 임대가 반환되어 재접속 경합이 생긴다.
- 샌드박스 안에서는 로컬 리스너 바인드가 `os error 5`로 거부되므로 이 실행에는
  승격 승인이 필요하다.
- 기본 포트 47800은 이전 Claude 세대가 점유했었다. 별도 포트 47803에서 격리 실행해
  런타임 제어를 검증했다.

## 2026-08-22 런타임 재검증

- PCSX2 v2.6.3, `pcsx2-rust-pine`, 계약 v3 검증 통과.
- ROM 식별: `SLPM-65254`, game version `1.01`, disc CRC `0aea02c5`.
- `pause`, 프레임 `step`, `tap`, EE `read_memory`, `find_pattern`, `screenshot` 동작 확인.
- 안정 캡처: `work/galaxy_angel/runtime_frozen.png` (640x480, frame 1473).
- 시작 시 시스템 데이터 로드 확인창의 표시 문구를 Shift-JIS로 EE RAM 전체 검색했으나
  전체 문장은 0건이었다. `システム` 짧은 패턴 4건은 모두 ELF의 디버그/메뉴 문자열
  영역(`0x3175b9`, `0x319fc0`, `0x319ff8`, `0x31a020`)이었다. 이 확인창은 본문 대사
  렌더 경로를 대표하지 않거나, 문자열이 즉시 변환·폐기되는 것으로 보인다.
- 짧은 `circle`/`cross` 입력은 전달됐으나 확인창의 글자 페이드 애니메이션만 관찰됐고
  일반 시나리오로 넘어가지 않았다. 다음 실행에서는 지속 입력(`set_input` 계열) 또는
  15프레임 이내 반복 홀드로 확인창을 통과해야 한다.
- 조사 세대 `launch-01m0krdnk4jnr8tgdtvs4tya3g`는 `stop`으로 정확히 종료했다.

## 2026-08-22 런타임 추적 진전

- 사용자가 기존 세대를 종료한 뒤, 지속형 MCP 세션(포트 47800)에서 새 관리 세대
  `launch-01m0krtba23te79p93prtsyb92`를 실행했다.
- 시작 확인창은 `left`로 `はい`를 선택한 뒤 `start`로 통과한다. `circle`/`cross`는
  이 확인창의 확정 키가 아니었다.
- 타이틀의 `はじめから`는 `circle`로 선택되며, 오프닝 영상 진입까지 확인했다.
  관련 캡처: `runtime_start_menu.png`, `after_left.png`, `post_boot.png`,
  `after_title_circle.png`.
- 시나리오 원문 `ところが、その文明にも、やがて危機がおとずれ`의 Shift-JIS 바이트열을
  EE 전체에서 검색해 **단일 일치**를 찾았다.
  - 주소: `0x01092DC8` (decimal `17378760`)
  - 이는 압축 해제된 실제 시나리오 문자열이 런타임 메모리에 상주함을 직접 확인한 것.
- 이 문자열 범위 `0x01092DC8..0x01092DFF`에 `read`, `pause_on_hit=true` 중단점(id 1)을
  설정했다. 오프닝 영상 동안에는 아직 이 범위 접근이 발생하지 않았으며, 이후 본문
  대사 표시 또는 영상 스킵 시점에서 중단점 이벤트와 호출 PC를 수집하면 된다.
- `call_stack`은 현재 PCSX2 포크에서 빈 스택을 반환할 수 있으므로, 중단점 적중 뒤에는
  이벤트의 PC와 `disassemble` 결과도 함께 확보해야 한다.

## 2026-08-22 대사 화면 고정점

- 실제 오프닝 내레이션 대사 화면까지 진입했다. 캡처는
  `work/galaxy_angel/trace_pulse.png`이며, 화면에는
  `人類が星々の海を行き来するようになってから、…`가 표시된다.
- 이 시점의 재개용 상태를
  `work/galaxy_angel/dialogue_font_trace.p2s`에 저장했다. 이후 조사는 이 상태를
  불러와 부팅/오프닝을 반복하지 않는다.
- 현재 화면의 첫 문장 앞부분은 EE에서 `0x01092C8F` 부근으로 확인됐다.
- 두 시나리오 문자열 버퍼에 read breakpoint를 설정해도 PCSX2 브리지의 이벤트는
  적중하지 않았다. 따라서 문자 표시 경로가 CPU의 일반 메모리 read로 관측되지 않거나,
  브리지의 PS2 read breakpoint가 이 형태의 접근을 잡지 못하는 것으로 판정한다.
  다음 추적은 문자열 read 중단점 반복이 아니라, 대사 화면 상태에서의 **glyph atlas /
  GS 텍스처 데이터 변화와 문자열 변환 전후 버퍼**를 비교하는 방식으로 전환한다.

## 다음 단계

1. 사용자가 지정한 최신 `create-retro-game-kr-patch` main을
   `vendor/create-retro-game-kr-patch/`에 복제했고, 해당 글꼴 전략·PoC·재삽입 기준을
   현재 프로젝트에 적용한다.
2. 저장 상태에서 대사 버퍼 `0x01092C00..0x01092FFF`의 write breakpoint를 설정해
   대사 전환을 시도했다. 브리지 이벤트는 적중하지 않았지만, 동결 호출 스택은 유효하다.
   실행 후보 프레임은 `0x00145D3C`, `0x00146028`, `0x00152768`, `0x00152238`이다.
   다음에는 이 후보를 exec breakpoint로 좁혀 객체 포인터·GS 전송 호출을 분리한다.
3. 문자열 read/write breakpoint를 계속 반복하지 않는다.
4. 해당 호출 전후 또는 텍스트 렌더 시점의 EE 메모리/GS 출력을 조사해
   본문 폰트 버퍼와 문자→글리프 변환 함수를 특정한다.
5. 본문 폰트 공급원이 확인되면 한글 셀 하이재킹 가능 여부를 결정한다.
6. 한글 패치 후 높이 눈금과 일본어 전각 눈금으로 글상자 폭/행 수/넘침 거동을 실측한다.
7. 그 뒤 용어집을 사용자와 확정하고 번역 승인을 받는다.

## 2026-08-22 시나리오 소비 경계 PoC

- 현재 화면의 원문은 네 종류의 복사본으로 관측된다.
  - 시나리오 원문: `0x01092CEF` / 다음 페이지 `0x01092DC8`
  - `CGMan::ResetText` 표식이 붙은 작업 영역: `0x010929A0`
  - 0x204바이트 슬롯 배열의 런타임 복사본: `0x00A6FC30`, `0x00A72A1C` 계열
- 이미 표시된 페이지의 원문 버퍼 `0x01092C8F`를 `TEST\0`로 바꿔도 화면은 변하지 않았다.
  텍스트 객체가 생성된 뒤에는 원문이 아니라 캐시된 렌더 객체를 사용한다는 증거다.
  캡처: `poc_runtime_text_edit.png`.
- 저장 상태를 다시 불러온 뒤, 아직 소비되지 않은 다음 페이지 원문
  `0x01092DC8`의 첫 15바이트를 `TEST TEST TEST\0`로 바꾸고 페이지를 진행했다.
  실제 대사창 첫 줄이 `TEST TEST TEST`로 표시됐다.
  캡처: `poc_preparse_ascii_2.png`.
- 따라서 시나리오 추출→수정→파싱→실제 대사창 렌더 경로는 검증됐다. 재삽입은
  렌더 후 RAM 패치가 아니라 압축 해제 전 시나리오 자산을 고쳐야 한다.
- 런타임 슬롯 갱신 시 `0x00A6FC30` 쓰기 중단점이 적중했다. 첫 적중은
  `0x0012DE6C`의 17개 슬롯(각 0x204바이트) 초기화 루프가 호출한 `memset` 내부
  `0x002BEC9C`였다. 이 슬롯은 대사/백로그 객체 풀로 판단한다.
- `0x010929A0` 쓰기 적중 PC `0x00156768`은 `0x20000000` 비캐시 별칭으로 64바이트씩
  기록하는 캐시 writeback 루틴이다. 문자 변환 함수가 아니므로 후보에서 제외한다.
- `0x00152238`은 프레임마다 적중하는 상위 UI 루프다. `0x00145D3C`와
  `0x00146028`도 범용 하위 경로로 확인되어 직접적인 글리프 변환 후보에서 제외한다.

## 현재 남은 구현 관문

1. Shift-JIS 코드가 일본어 글리프 비트맵/텍스처 좌표로 바뀌는 함수와 본문 폰트
   공급원을 특정한다.
2. 사용하지 않는 2바이트 코드 영역에 완성형 한글 글리프를 배정하는 PoC를 만들고,
   실제 대사창에서 한글 한 글자 이상을 표시한다.
3. 시나리오 재압축·컨테이너 재삽입·ISO 빌드 후 동일 화면에서 재검증한다.
4. 위 세 단계가 끝나면 용어집과 번역 파일만 다루는 번역 단계로 전환한다.

## 2026-08-24 첫 선택지 이후 0177 정지 원인

- v21/v23 계열 첫 시나리오의 구조 레코드를 원본과 대조한 결과, 선택지 앞의
  `\\Speaker(001)`과 `\\Face(001,0,223,0)` 인자에 수백 개의 `0`이 삽입돼 있었다.
- 과거 앵커 위치 보정이 명령 인자 자체를 늘린 것이며, 선택지는 표시되더라도 이후
  0177 진입에서 런타임 상태가 깨지는 원인이 된다.
- 현재 `galaxy_angel_translation.py`는 구조 명령을 수정하지 않도록 되어 있으며,
  원본과 구조 레코드 973개가 모두 일치하는 첫 시나리오를 v21의 정상 PIDX/관리 블록
  배치에 이식한 `Galaxy Angel (Korean) v29 clean-commands.iso`를 만들었다.
- v29는 현재 전체 빌드의 첫 시나리오를 통째로 이식해 시작부 0002에서 회귀했다.
  폐기 대상으로 본다.
- v30은 정상적으로 시작·선택지까지 진행되던 v21을 기반으로 하며, 손상된 두 명령만
  복구했다. 제거한 822바이트는 파싱 종료 뒤 블록 꼬리 패딩으로 옮겼다. 따라서 0002를
  포함한 대사/선택지 데이터는 v21과 동일하고 구조 레코드 973개는 원본과 일치한다.
- v30 이후 0180 진입 정지를 계기로 전 시나리오를 검사했다. 원본 ISO와 PIDX 레코드
  기준으로 대응되는 231개 블록을 비교한 결과 148개 블록에서 숫자 인자에 `0`을 넣은
  명령 손상이 발견됐다. `Face`, `Speaker`, `Voice`, `SelStart`, `SceneChg`, `BGM` 등이
  포함됐다.
- `galaxy_angel_repair_all_structure.py`는 대사 본문과 선택지 문장만 패치 ISO에서
  가져오고, 그 밖의 명령·앵커·마커·개행은 원본 바이트로 재조립한다.
- v32에서 231개 블록 구조 검사 결과 `problems=0`을 달성했다. v33은 여기에 44열
  경계였던 0180을 36/38/10열로 재배치했으며 재검사도 `problems=0`이다.
- v34에서 0180 본문을 일본어 원문으로 복원해도 같은 위치에서 정지했다. 본문이나
  줄 폭이 원인이 아니었다.
- 근본 원인은 번역으로 제어 흐름 마커의 바이트 위치가 이동한 것이다. 첫 블록의
  마지막 분기 `}`는 원본 `0x59B2`에서 패치 `0x55B6`으로 이동했고, 0180 본문도
  원본 `0x5ED0`에서 패치 `0x5A62`로 1,134바이트 앞당겨졌다. 선택 분기의 원본 복귀
  위치가 번역문 중간을 가리키게 된다.
- `galaxy_angel_restore_control_offsets.py`를 추가했다. 모든 `<group->IDS`, `:L`, `[`,
  `]{`, `}`, `}/` 위치를 원본과 맞추되, 예전처럼 한 명령에 수백 개의 0을 넣지 않고
  64바이트 이하의 여러 숫자 명령 인자에 소량씩 분산한다. 부족한 구간은 번역 본문의
  ASCII 구두점/공백을 동등한 CP932 전각 문자로 바꿔 1바이트씩 보충한다.
- v36은 재배치 블록 덮어쓰기 결함까지 수정한 결과물이다. 원본과 대응되는 231개
  블록의 모든 제어 흐름 마커 종류와 오프셋을 검사해 `problems=0`을 통과했다. 첫
  블록의 세 `}` 위치도 원본과 동일한 `0x4C8F`, `0x4F98`, `0x59B2`다.
- v36은 숫자 명령 인자에 소량의 선행 0을 분산했지만 `Voice(...,0003)` 등도 엔진에서
  원본과 동등하지 않아 0175 앞에서 정지했다. 숫자 인자를 건드리는 방식은 폐기했다.
- v38은 제어 오프셋 보정에 명령을 전혀 사용하지 않는다. 대사 각 행은 인코딩 기준
  44바이트, 선택지 행은 34바이트를 넘지 않는 범위에서 후행 공백을 분산하고, 필요한
  경우 3줄 한도 안에서 공백 행을 추가한다. 마지막 가상 행에 공백이 붙어 `@@)`가
  밀리던 v37 결함도 수정했다.
- v38 검증: 231개 블록의 구조 명령 비교 `problems=0`, 제어 마커 오프셋 비교
  `problems=0`. 0175 앞 명령도 `SceneChg(3,590,0,0,0,0,0)`, `Speaker(005)`,
  `Face(142,0,0,0)`, `Voice(GA101113,3)`로 원본과 정확히 일치한다.
- 사용자 실기에서는 v38도 초반 내레이션에서 정지했다. 표시 문자열에 후행 공백이나
  공백 행을 넣어 오프셋을 맞추는 방식 역시 파서 입력을 바꾸므로 폐기한다.
- v39는 최선 기준판 v30의 첫 시나리오 압축 해제 바이트를 전혀 수정하지 않았다.
  v30과 IDS0101 raw SHA-256이 동일한
  `31f762267f876836a0575e937b648183cbed7ee5258e0b085f31efe15c2d16dc`임을 확인했다.
  변경점은 해당 블록을 LZSS 참조 토큰 없이 리터럴 토큰만으로 재압축한 것뿐이다.
  게임의 실제 해제기와 자체 Python 압축기의 후반부 해석 차이 가능성을 검증하기 위한
  판이며, 대사·명령·호출 위치·종료 위치는 v30과 완전히 동일하다.

## 2026-08-24 사용자 실기 확인 기준 안정판

- 현재까지 사용자가 직접 확인한 판 중 **v30이 가장 문제가 적고 가장 멀리 진행된
  기준판**이다. 이후 수정은 v30을 기준으로 최소 변경해야 한다.
- v38은 정적 검사에서 구조 명령과 제어 마커 오프셋이 일치했지만, 실제 게임에서는
  초반 내레이션에서 정지했다. 따라서 v38의 후행 공백/공백 행 기반 오프셋 보정은
  런타임 파서와 호환되지 않으며 폐기한다.
- v38은 일본 원본과 런타임 메모리가 동일하지 않다. 동일하게 맞춘 것은 압축 해제
  블록의 총 크기와 선택한 제어 마커의 오프셋뿐이다. 번역 문자열, 후행 공백, 추가
  공백 행, 한글 글꼴 ELF 데이터는 원본과 다르므로 실제 파서 입력과 메모리 내용도
  다르다.
- 모든 스크립트 메모리 바이트를 일본 원본과 완전히 동일하게 하면 원본과 같은 실행
  결과가 나오지만 번역문도 사라진다. 다음 수정은 공백이나 명령 인자 보정 없이 v30의
  정상 동작 구간을 보존하고, 정지 지점에 직접 영향을 주는 최소 바이트만 조사한다.

## 2026-08-22 외자 코드 탐색

- `font.tbl` 원문은 `[FONT_EX]`에 외자 번호 1~31을 선언할 수 있다고 명시한다.
  현재 #01~#05는 `HEART`, `RETURN`, `TALK`, `CIRCLE`, `TRIANGLE`이며 #01은
  `pjfont00.t32`를 사용한다. 이 외자 슬롯을 늘려 한글을 공급하는 경로가 가장 유력하다.
- CP932 사용자 정의 문자 첫 코드 `F040`을 다음 페이지에 주입했으나 가시 글리프는
  나오지 않았다(`poc_gaiji_f040.png`). 원시 제어 바이트 `01..05`도 가시 글리프가
  아니었다(`poc_gaiji_ctrl_01_05.png`). 따라서 외자 번호는 이 두 단순 인코딩으로
  직접 호출되지 않으며, 엔진의 별도 이스케이프/매핑 규칙을 더 찾아야 한다.
- CP932 사용자 정의 문자 `F040..F09F` 95개를 한 화면에 연속 주입했으나 전부 공백으로
  처리됐다(`poc_gaiji_f0_row.png`). `FA40`, `EB40`, `8740`도 가시 외자가 아니었다.
  따라서 `[FONT_EX]` 번호는 CP932 PUA를 단순 순서 매핑한 구조가 아니다.
- ELF 파일 `0x26E000` 이후의 저엔트로피 대형 데이터를 1bpp/4bpp 글리프 셀로
  시각화했지만 문자 형태가 나오지 않았다. 대응 VA `0x0036D000..0x0036DFFF`에
  read breakpoint를 걸어 새 대사를 생성해도 접근이 없었다. 이 영역은 현재 대사
  폰트 공급원이 아니므로 후보에서 제외한다.

## 2026-08-24 시나리오 인덱스 근본 수정 및 전체 잔여 텍스트 번역

- 화면에 `0,3,0,0)` 또는 `ce(021,0,046,0)`가 노출되던 원인은 JSON의 `Face` 값이
  아니라 GADAT001의 별도 인덱스 블록(`0x14E000`)이 번역 전 일본어 문자열의 바이트
  위치를 계속 가리킨 것이었다. 분기 결과가 `\\Face(...)` 명령 중간으로 진입했다.
- `galaxy_angel_translation.py`에서 원문 바이트 위치를 맞추기 위해 대사/명령을
  변형하던 처리를 제거했다. 시나리오 종료 뒤 패딩만으로 블록 크기를 보존한다.
- `galaxy_angel_build.py`에 번역 후 실제 `IDSxxxx:{`와 `:Lxxxx` 위치를 수집해
  GADAT001 인덱스를 재작성하는 처리를 추가했다. 숫자 필드는 선행 0을 사용해 원래
  인덱스 블록 크기를 유지하며 로컬 PIDX와 중앙 `IDX.DAT` 양쪽에 반영한다.
- 수정 ISO: `build/Galaxy Angel (Korean).iso`
  - SHA-256: `a12f85158aec5b0f54a2d71de69cc867a9ad7b2ab41aaf1ad9a3b02cad19c4f0`
  - 인덱스 대상 1,135개 해소, 910개 위치 갱신
  - 완성 ISO의 시나리오 210/210 블록 일치
  - 구조 검사 231개, 문제 0개
- 용어를 `알모` → `아르모`, `ローム星系` → `롬 성계`로 통일했으며 용어집,
  화자명 표, 번역 JSON 전체에 반영했다.
- 새로 추출한 전투 대사 2,302개와 기타 압축 텍스트 859개를 로컬
  `gemma-4-26b-a4b-it-qat`로 번역했다. 실행 파일/비압축 검토 후보 1,070개도
  번역문을 모두 채웠지만 개발 로그·문자표·오검출이 섞여 있으므로
  `use_translation: false`를 유지한다.
- `galaxy_angel_sync_extracted_translations.py`를 추가해 고유 번역을 전투 대사
  37,805개 occurrence와 164개 파일, 기타 압축 텍스트 859개/3개 컨테이너 파일에
  동기화했다.
- 최종 JSON 검수 범위는 170개 파일/80,700개 항목이다. 빈 번역, 번역문 내 일본어,
  마지막 개행 불일치, printf/RAW 자리표시자 불일치, `translation_error`가 모두 0개다.

## 2026-08-24 선택지 고정 위치 인덱스 미러 수정

- GADAT001은 번역 후 원래 ISO 슬롯보다 40,960바이트 커져 새 extent로 이동한다.
  일반 시나리오는 ISO9660 디렉터리의 새 위치를 따라가지만, 선택지의
  `<9999->IDSxxxx>` 관리 경로는 원래 고정 sector의 보호 영역을 참조할 수 있다.
- 기존 빌드는 이동된 GADAT001의 `0x14E000` 인덱스만 재구축하고 원래 extent에는
  일본어 기준 인덱스를 남겼다. 이 때문에 일반 대사는 정상 표시되다가 선택지에서
  예전 문자열 오프셋으로 진입해 멈출 수 있었다.
- `galaxy_angel_build.py`가 GADAT001을 이동할 때 보호 인덱스 압축 블록과 해당 로컬
  PIDX 튜플을 원래 extent에도 미러하도록 수정했다.
- 새 ISO: `build/Galaxy Angel (Korean).iso`
  - SHA-256: `5d338d54b65ff38c5178db4ccfe78d9b284ab9a2c2be8820bf4f421fa564e885`
  - 원래 extent `1002548`, 활성 extent `2026560`의 인덱스 원문·압축 데이터 및
    로컬 PIDX 튜플 동일 확인
  - 시나리오 구조 검사 231개, 문제 0개

## 2026-08-24 v41 외부 시나리오 호출 인라인 ROM 개조

- 인덱스와 컨테이너 위치를 모두 보정해도 첫 선택지에서 멈추므로, 외부 관리 그룹
  호출을 유지하는 접근을 중단했다.
- 첫 선택지 직전 `<9999->IDS2100`은 원본 관리 루틴에서 `w008`~`w013`을 1로
  설정하는 호출이다. v20에서 이 호출을 직접 명령으로 바꾸면 선택지가 표시됐던
  실기 결과를 전체 시나리오에 일반화했다.
- `galaxy_angel_inline_external_calls.py`를 추가했다. 원본 ISO의 관리 그룹
  9995~9999를 해제해 루틴 본문을 읽고, 안전하게 펼칠 수 있는 외부 호출을 호출
  위치의 원본 명령으로 대체한다.
- 전체 210개 시나리오에서 외부 호출 247개를 인라인했다. 특히 9999 그룹의
  `IDS2100`~`IDS2499` 선택지 플래그 호출은 남은 것이 0개다. 로컬 라벨 흐름을
  포함한 복잡 호출 93개는 의미 훼손을 피하기 위해 그대로 유지했다.
- 인라인으로 라벨 오프셋의 십진수 자릿수가 늘어나는 경우를 지원하도록 텍스트형
  GADAT001 인덱스 필드를 가변 길이로 재구축한다.
- 완성 ISO에서 210/210 해제 시나리오가 인라인 빌드 입력과 일치하고, 선택지 외부
  호출 0개, 활성/고정 위치 인덱스 일치, 재생성 기대 인덱스 일치를 확인했다.
- 테스트 ISO: `build/Galaxy Angel (Korean) v41 inline-calls.iso`
  - SHA-256: `73c6705c647194e1b85691d8734e34a4eb75597cfb11d2607e241ba4b6ff66b7`

## 2026-08-24 v42 GADAT001 고정 베이스 근본 수정

- PCSX2 PID 10444의 EE 메모리를 정지 지점에서 조사했다. `라져!` 다음 스크립트는
  `\BGM`, `\gFade`, `<9998->IDS3100`, 변수 설정, `\exit(2)` 순서였으며,
  런타임 메모리 어디에도 호출 대상 `IDS3100:{`이 로드되어 있지 않았다.
- 원인은 GADAT001 전체를 원래 extent `1002548`에서 `2026560`으로 옮긴 것이다.
  일반 대사는 ISO9660의 새 위치를 따르지만, 그룹 9995~9999 외부 관리 호출은 원래
  고정 컨테이너 베이스를 사용하므로 선택지와 전투 전환에서 대상 루틴을 읽지 못했다.
- `galaxy_angel_build.py`를 수정해 GADAT001의 전체 로컬 PIDX, 관리 루틴, 일반
  시나리오를 원래 extent에 유지한다. 원래 할당을 넘는 시나리오 블록 2개만 ISO
  뒤쪽 backing copy로 보내고 해당 두 PIDX/IDX 튜플만 큰 상대 오프셋으로 연결한다.
- 외부 호출 인라인은 공식 빌드에서 제거했다. 번역문 외의 원본 스크립트 구조와
  `<9998->IDS3100>` 같은 호출을 그대로 유지한다.
- `galaxy_angel_verify_stable_iso.py`를 추가해 큰 논리 extent를 메모리에 복사하지
  않고 다음을 전수 검증한다.
  - GADAT001 extent `1002548` 유지
  - 번역 시나리오 210/210 압축 해제 결과 일치
  - 초과 블록 2개만 backing 영역 사용
  - 로컬 PIDX와 중앙 IDX.DAT 318/318 튜플 일치
  - 관리 블록 `0x148800`의 `IDS3100` 루틴 보존
- 테스트 ISO: `build/Galaxy Angel (Korean) v42 stable-base.iso`
  - SHA-256: `41a18c56aa48d9546f5f878f3558355c340637ca4b6e5e84e1e436a9d83715dc`
  - 검증 결과: `scenarios=210 redirected=2 idx_records=318 IDS3100=True`

## 2026-08-24 v43 전체 외부 루틴 개조 검증

- “2개”는 처리한 루틴 수가 아니라 원래 GADAT001 물리 할당을 넘겨 backing 영역에
  저장한 압축 블록 수다. 고정 베이스 개조는 특정 선택지나 IDS3100 하나가 아니라
  GADAT001의 전체 PIDX 318개와 관리 그룹 9995~9999 모두에 적용된다.
- `galaxy_angel_verify_stable_iso.py`를 전체 호출 그래프 검사기로 확장했다. 일반
  시나리오 외부 호출 337개와 관리 루틴 내부 연쇄 호출 316개를 모두 수집하며,
  대상 루틴이 없거나 압축 블록·로컬 PIDX·중앙 IDX 중 하나라도 어긋나면 실패한다.
- 관리 그룹 내부의 로컬 루틴 호출 27개와 로컬/랜덤 분기 라벨 참조 166개도 모두
  대상 존재를 검사한다. 전체 선택지 블록 339개는 `@(ss`/`\SelStart`/`@)ss` 세
  구조 마커 수가 정확히 일치하는지 검사한다.
- 그룹별 일반 호출/고유 대상/수록 루틴 검증 결과:
  - 9995 공통: `10 / 5 / 6`
  - 9996 진행·상태: `85 / 73 / 107`
  - 9997 이벤트·결과: `27 / 4 / 10`
  - 9998 전투 준비·판정: `24 / 20 / 38`
  - 9999 선택지·공통 플래그: `191 / 155 / 168`
- `build_patch.ps1` 마지막 단계에 이 전수 검사를 필수 빌드 게이트로 추가했다.
  앞으로 모든 선택지, 모든 전투 준비/결과 및 기타 관리 호출 중 하나라도 연결되지
  않으면 완성 ISO로 간주하지 않는다.
- 테스트 ISO: `build/Galaxy Angel (Korean) v43 all-routines.iso`
  - SHA-256: `41a18c56aa48d9546f5f878f3558355c340637ca4b6e5e84e1e436a9d83715dc`

## 2026-08-24 v44 전투 대사 실제 삽입

- 전투 대사 2,302개/출현 37,805개는 JSON 번역과 occurrence 동기화까지만 되어
  있었고 기존 `build_patch.ps1`은 GADAT002와 SLGINIT를 패치하지 않았다. 게임에서
  일본어가 나온 직접 원인이다.
- `galaxy_angel_build_battle.py`를 추가해 GADAT002의 `[MESSAGE*]` 대사와 기타
  전투 화면 문구를 같은 해제 블록에 함께 적용하고 재압축한다.
- GADAT002 번역 블록은 262개다. 커진 45개 블록은 backing 영역으로 이동하고,
  고정 GADAT002 베이스의 로컬 PIDX와 중앙 IDX.DAT(file id 26)를 함께 갱신한다.
- SLGINIT는 22개 FSTS 테이블과 26,216개 압축 스트림으로 구성되어 있다. 번역된
  전투 복사본 628개 중 627개가 원래 슬롯보다 커서 단순 덮어쓰기가 불가능했다.
  번역 스트림을 ISO 뒤쪽에 저장하고 628개 FSTS 레코드의 offset/raw/compressed
  튜플을 갱신했다. 동일 데이터는 공유하여 실제 저장 스트림은 100개다.
- `galaxy_angel_font.py`가 전투 및 잔여 압축 텍스트도 수집하도록 수정했다. 전투
  전용 한글 10자를 포함해 커스텀 글리프 1,216개를 ELF에 패치한다.
- `galaxy_angel_verify_battle_iso.py`를 추가하고 공식 빌드의 필수 검사로 연결했다.
  완성 ISO에서 GADAT002 262개와 SLGINIT 복사본 628개를 다시 해제하여 기대 번역
  블록과 바이트 단위로 대조한다. PIDX/IDX 426개도 전부 일치한다.
- 번역 원본 검수: 전투 고유 2,302개와 GADAT002 기타 문구 623개에서 빈 번역 및
  번역문 내 일본어가 0개다.
- 최종 ISO: `build/Galaxy Angel (Korean) v44 battle-dialogue.iso`
  - SHA-256: `0642f7b65c177fa4260ecee6aa9c7983a808dfc3d03a40d84dc192cd3dd6156c`
  - 크기: `4,159,496,192`바이트
- 내부 LZSS 재압축은 게임이 읽을 데이터를 만드는 단계이며, 배포용 xdelta는 이
  완성 ISO와 원본 ISO의 차이를 마지막에 생성하는 별도 단계다.

## 2026-08-24 v45 SLGINIT 원본 크기 내부 재배치

- v44는 전투에 들어가자마자 게임오버 화면으로 넘어가는 실기 문제가 발생했다.
  번역 스트림을 ISO 끝으로 보낸 뒤 SLGINIT FSTS가 그 위치를 가리키게 하면서
  SLGINIT의 논리 크기가 약 2GB로 확장된 것이 원인 후보였다. 전투 초기화 시 파일
  전체를 다루는 경로와 호환되지 않는 방식이므로 폐기한다.
- SLGINIT는 22개 FSTS 뱅크, 26,216개 압축 스트림으로 구성된다. 각 뱅크 시작,
  FSTS 표, SLGINIT extent와 파일 크기는 원본 그대로 유지하면서 뱅크 내부 데이터
  영역만 전부 재패킹한다.
- 모든 FSTS 레코드의 offset/raw/compressed 값을 다시 만들고 동일 압축 스트림을
  뱅크 안에서 공유했다. 전투 복사본 628개는 188개 실제 스트림 주소를 사용한다.
- 최종 검증에서 GADAT002 번역 블록 262개, PIDX/IDX 426개, SLGINIT 복사본
  628개가 모두 기대 번역 데이터와 일치했다. SLGINIT extent `1106343`, 크기
  `73,911,040`바이트도 일본 원본과 동일하다. 검증기가 크기 변경도 거부하도록 했다.
- 최종 ISO: `build/Galaxy Angel (Korean) v45 battle-inplace.iso`
  - SHA-256: `7a8bb1d03d467dafe569c5aa1c7d9e896af1214300a39180e86df6eac6d59235`
  - 크기: `4,157,970,432`바이트

## 2026-08-24 v46 전투 공백 파서 및 튜토리얼 런타임 복사본

- 전투 대사가 `목표를 / 이제`처럼 첫 단어만 표시되는 원인은 `[MESSAGE]` 설정값
  파서가 ASCII 공백에서 값을 종료하기 때문이다. 공백이 거의 없는 일본어 원문에서는
  드러나지 않았으나 띄어쓰기가 있는 한국어에서는 각 행이 첫 공백에서 잘렸다.
- 전투 및 GADAT002 기타 화면 문구를 삽입할 때 ASCII 공백을 CP932 전각 공백
  `0x8140`으로 바꾸도록 했다. 화면에서는 공백으로 보이지만 설정 파서는 한 행의
  일부로 읽는다.
- 전투 진입 튜토리얼은 번역된 GADAT002가 아니라 SLGRES.DAT의 런타임 복사본을
  사용했다. GADAT002 잔여 문구 occurrence를 원본 해제 블록 해시로 대응해
  SLGINIT 166개 블록과 SLGRES 1개 블록까지 자동 연결하도록 확장했다.
- 최종 런타임 적용/검증은 SLGINIT 794블록, SLGRES 1블록, 합계 795블록이다.
  두 파일 모두 원본 extent와 논리 크기를 유지하며 FSTS 뱅크 내부에서 재패킹한다.
- SLGRES 튜토리얼 뱅크는 일반 압축으로 132바이트 넘쳐 최적 토큰 경로 압축을
  적용했다. 번역문을 줄이거나 자르지 않고 두 스트림에서 118바이트와 22바이트를
  절약해 원본 뱅크 크기에 맞췄다.
- 완성 ISO의 SLGRES 튜토리얼 해제 데이터에서 `마이어스　대령,　고속　링크…`
  번역 바이트와 ASCII 공백 0개를 확인했다.
- 최종 ISO: `build/Galaxy Angel (Korean) v46 battle-spacing-tutorial.iso`
  - SHA-256: `88355ac7b7bb246bbd8f53f0af4f5029511bb1ed37685d12e9ada0f5e3ae41b2`
  - 크기: `4,158,031,872`바이트

## 2026-08-24 v47 튜토리얼 겹낫표 복원

- 일본어 튜토리얼 원문은 강조 용어에 겹낫표 `『 』`를 사용한다. 번역기 지침도
  이를 유지하도록 되어 있으나 4개 문장에서만 `''카메라 뷰''`, `''맵 뷰''`처럼
  ASCII 작은따옴표 두 개씩으로 잘못 바뀌어 기호가 넓고 어색하게 표시됐다.
- 해당 4개를 `『카메라 뷰』`, `『맵 뷰』`로 고치고 고유 번역 파일과 GADAT002
  컨테이너별 파일을 함께 갱신했다. 두 파일의 `''` 잔존은 0개다.
- GADAT002, SLGINIT, SLGRES를 다시 만들고 전투/튜토리얼 런타임 복사본 795개를
  전수 검증했다.
- 최종 ISO: `build/Galaxy Angel (Korean) v47 tutorial-brackets.iso`
  - SHA-256: `24f83dac968c9ac80906edf60d3ac30c8017d73bb014268b17a67e7b1c9dd3a4`

## 2026-08-24 v48 전투 반각 공백 및 직역 교정

- v46의 파서 회피용 전각 공백 `0x8140`은 문장을 자르지는 않았지만 화면에서
  한글 한 칸 너비를 차지해 전투 대사와 튜토리얼이 지나치게 벌어졌다.
- 전투 설정값의 구분자로 처리되는 ASCII `0x20` 대신, 설정값을 끊지 않으면서
  폰트 경로에서 반각 빈칸 슬롯으로 쓰이는 단일 바이트 `0xA0`을 사용하도록
  `galaxy_angel_build_battle.py`에 전투 전용 인코더를 추가했다. 번역 JSON에는
  일반 ASCII 공백을 유지하고 ISO 삽입 시에만 변환한다.
- 의역된 `了解でーす。`의 `알겠어용.`을 직역 기준 `알겠습니다~`로 교정했다.
  같은 유형으로 확인된 전투 및 시나리오의 `미안해용!` 2건도 `미안해요!`로
  고치고 전투 발생 위치 75,610개를 다시 동기화했다. 전체 번역 데이터에서
  `알겠어용`/`미안해용` 잔존은 0건이다.
- GADAT002 전투 블록 262개와 SLGINIT/SLGRES 런타임 복사본 795개를 다시
  만들고 바이트 단위 전수 검증했다. 선택지/전투 준비를 포함한 시나리오 구조
  검증도 함께 통과했다.
- 최종 ISO: `build/Galaxy Angel (Korean) v48 battle-halfspace-literal.iso`
  - SHA-256: `29fb2d69ea8b4c00a0a5e58ea68a3991f08da17814d7b3bedb9ad91a4b56da78`
  - 크기: `4,157,970,432`바이트

## 2026-08-24 v49 ADV 런타임 시나리오 인덱스

- 전투 복귀 뒤 메시지 로그에 `oice(GA104007,3)`, `09,3)`처럼 스크립트 명령
  일부가 대사로 표시됐다. 해당 장면의 `L0140001`은 원본 1373바이트에서 번역 후
  1309바이트로, `L0140002`는 1718에서 1643바이트로 이동했다. 메인 GADAT001
  인덱스는 새 위치였지만 ADV.DAT의 런타임 복사본은 원본 위치를 유지해 명령
  중간으로 실행이 진입한 것이 원인이었다.
- `galaxy_angel_build.py`가 ADV/MISC 압축 스트림을 검사해 원본 시나리오 인덱스와
  동일한 런타임 복사본을 자동으로 찾아 재구축된 인덱스로 교체하도록 했다. 이번
  빌드에서는 ADV.DAT의 28,704바이트 인덱스 복사본 1개가 패치됐다.
- `galaxy_angel_verify_stable_iso.py`에 런타임 인덱스 검증을 추가했다. 메인
  GADAT001 인덱스와 런타임 복사본의 해제 결과가 바이트 단위로 다르거나 복사본을
  찾지 못하면 앞으로 빌드를 실패시킨다.
- 별도 전체 ISO 감사도 수행했다. 원본의 완전한 인덱스 스트림은
  `GADAT001.DAT +0x14E000`과 `ADV.DAT +0xEA40` 두 곳뿐이었으며, v49에서는
  두 곳 모두 새 인덱스로 교체됐다. v49 전체 ISO에서 원본 인덱스 스트림 잔존은
  0개다. `galaxy_angel_audit_runtime_indexes.py`를 `build_patch.ps1`의 필수
  빌드 게이트로 연결해 이후에도 전체 ISO를 검사한다.
- 시나리오 210개, 선택지 339개, 외부 호출 337개, 전투 블록 262개 및 전투
  런타임 복사본 795개를 다시 전수 검증했다.
- 최종 ISO: `build/Galaxy Angel (Korean) v49 runtime-index.iso`
  - SHA-256: `89936bf4564c5e4c52aa8646a188f796d9d6436bea92eb7727ed10ce79e1004a`
  - 크기: `4,157,970,432`바이트

## 2026-08-24 v50 세이브 슬롯 제목

- 세이브 슬롯의 날짜 뒤에 표시되는 장 제목은 GADAT000 `+0x1B800`의
  `[SAVELOAD]` 텍스트였다. 기존 잔여 번역에는 90개가 번역돼 있었지만 빌드에
  연결되지 않았고, 번호가 `#x10`인 각 장의 첫 항목 15개는 추출 규칙에서 메타
  필드로 잘못 제외돼 있었다.
- 15개 장의 `(1)~(5)`, `★클리어`, `(0)` 총 105개 제목을 모두 한국어로
  재구축했다. `MISC.DAT +0x237E40`의 런타임 복사본 1개도 같은 데이터로 패치했다.
- 새 SAVELOAD 스트림은 일반 압축 시 원본 슬롯보다 6바이트 컸으나 최적 압축으로
  `1455 -> 1314`바이트가 되어 원본 슬롯 안에 안전하게 들어갔다.
- ELF의 세이브 행 포맷이 별도 출력하는 `第/章` 4개도 `제/장`으로 교체했다.
  상단 `セーブ` 및 `閉じる` 버튼은 `GADAT032`의 화면별 이미지 자산이다.
- 전투 커맨드의 설정 문자열은 GADAT002/SLGINIT에서 이미 한국어로 패치·검증된
  상태인데 화면에는 일본어 픽셀 글자가 유지된다. 실제 표시부는 GADAT032의
  전투 UI 이미지 자산이며 일반 전투 텍스트 경로와 다르다.
- 최종 ISO: `build/Galaxy Angel (Korean) v50 save-titles.iso`
  - SHA-256: `6bd430ad9dd31559b7742d4f4ae24b1a07bdb10e9d77d22ddc5d6df49d790e8c`
  - 크기: `4,157,970,432`바이트

## 2026-08-24 GADAT032 일본어 이미지 추출

- GADAT032의 PIDX 이름표와 데이터 레코드 순서를 대응시켜 이름이 있는 자산
  2,378개를 `assets/image_extraction/GADAT032/raw/`에 추출했다.
- TEX의 32비트 RGBA, 8/4비트 색인+PS2 CLUT, 24비트 RGB, 640x480 분할 전송
  형식을 구현해 TEX 1,843개 전부를 PNG로 변환했다.
- 23장의 전체 미리보기를 시각 검수하고 일본어 글자가 실제로 들어간 이미지
  421개를 `assets/image_extraction/japanese_images/`에 별도 분류했다. 원본 TEX,
  PNG, JSON/TSV 목록 및 파일명 포함 미리보기 6장을 함께 보존했다.
- 포함 범위: 장소명, 시스템 메시지, 세이브/로드·설정·타이틀 메뉴, 전투 명령과
  도움말·함선명, 유닛/작전/전투 결과 화면, 미니게임, 앨범 곡명, 11개 장 번호와
  15개 장 제목.
- 재현 도구:
  - `tools/galaxy_angel_extract_gadat032.py`
  - `tools/galaxy_angel_make_texture_contact_sheets.py`
  - `tools/galaxy_angel_collect_japanese_images.py`

## 2026-08-24 v51 단일 UI 이미지 재패킹 시험

- 사용자가 수정한 `assets/image_extraction/GADAT032/pguact02.png`(전투 명령
  `공격`)를 원본 64x16 선형 RGBA TEX로 역변환했다. PS2 GS 알파 범위도
  PNG의 0~255에서 원본 0~0x80으로 되돌렸다.
- 재압축 크기는 869바이트에서 1,192바이트가 됐으며 원래 정렬 슬롯 2,048바이트
  안에 들어가 컨테이너 내 다른 자산의 위치는 바뀌지 않았다.
- GADAT032 로컬 PIDX 레코드뿐 아니라 IDX.DAT 중앙 미러의 압축 크기도 함께
  갱신했다.
- 재현 도구: `tools/galaxy_angel_patch_gadat032_images.py`
- 테스트 ISO: `build/Galaxy Angel (Korean) v51 pguact02-image-test.iso`
  - SHA-256: `319063fdfd7d609b536b0bbf2a6b3e9d5d43b92c073160a918fd498718b4fae9`
  - 크기: `4,157,970,432`바이트
- 후속 확인: `pguact02`는 공격 글자를 담고 있지만 왼쪽 전투 명령 목록이 아니라
  유닛의 현재 행동 상태에 쓰이는 64x16 이미지다. 왼쪽 목록의 공격은
  `prbtn02`(보통), `prbtn02b`(비활성), `prbtn02f`(선택)이며, 별도 정보 화면에는
  `prbtn20/20f`가 쓰인다. 최초 설명에서 이 용도를 잘못 단정했다.
- v51을 다시 추출해 사용자가 준 PNG와 비교했으며 RGB 픽셀은 완전히 동일하고,
  40픽셀의 알파만 PS2 7비트 알파 왕복 특성으로 1단계 차이가 났다. 따라서
  `pguact02` 블록 패치 자체는 정상이다.

## 2026-08-24 v52 전투 명령 공격 이미지 시험

- 사용자가 수정한 `assets/image_extraction/GADAT032/prbtn02.png`를 v50에 단독
  적용했다. 이 자산은 왼쪽 전투 명령 목록의 일반 상태 `공격`이며, 선택 상태는
  별도 `prbtn02f`, 비활성 상태는 `prbtn02b`다.
- 원본 크기 108x20 및 TEX 원시 크기 8,704바이트를 유지했다. 압축 크기는
  2,520에서 2,896바이트로 증가했지만 원래 슬롯 4,096바이트 안에 들어갔다.
- 완성 ISO에서 GADAT032를 다시 추출해 수정 PNG와 대조했다. RGB 차이는 0픽셀,
  PS2 알파 양자화로 인한 최대 1단계 차이만 65픽셀에서 확인됐다.
- 테스트 ISO: `build/Galaxy Angel (Korean) v52 prbtn02-image-test.iso`
  - SHA-256: `c9bcce022464e71f3d5f34f68929a08d87dfd94aa46ef5ed2ab1c036d39726d2`
  - 크기: `4,157,970,432`바이트

## 2026-08-24 v53 전투 런타임 이미지 복사본

- v52가 바뀌지 않은 원인은 `prbtn02.tex`의 동일 압축 블록이 GADAT032 외에
  `SLGRES.DAT +0x142B60`에도 존재했기 때문이다. 전투 화면은 이 런타임
  복사본을 사용한다.
- SGLRES의 다음 압축 블록은 `+0x143540`에서 시작해 가용 슬롯이 2,528바이트다.
  사용자가 수정한 PNG를 그대로 최적 압축하면 2,879바이트라 들어가지 않았다.
  시험용으로 16색 양자화한 결과 1,356바이트가 되어 두 위치에 동일하게 삽입했다.
- GADAT032 로컬 PIDX와 IDX.DAT 중앙 레코드도 1,356바이트로 갱신했다. ISO
  전수 검색에서 새 압축 블록이 GADAT032와 SLGRES 두 곳에 존재함을 확인했다.
- 테스트 ISO: `build/Galaxy Angel (Korean) v53 prbtn02-runtime-test.iso`
  - SHA-256: `c7f4888e0e75c7d24e1e8316420e4547a5da5333a7cf3af962f9e872a01298a8`
  - 크기: `4,157,970,432`바이트

## 2026-08-24 이미지 한글화 자동화 및 전체 회귀 검증

- 정식 이미지 작업 폴더를 `assets/image_extraction/japanese_images/png/`로
  확정했다. 기준 원본은 `assets/image_extraction/GADAT032/png/`이며 빌드 시
  파일 바이트가 아니라 RGBA 픽셀 해시를 비교해 실제로 바뀐 PNG만 선택한다.
  현재 정식 작업 폴더의 변경 이미지는 0개이고, 루트에 둔 과거 시험 이미지는
  자동 빌드 대상이 아니므로 보존된 채 섞이지 않는다.
- `galaxy_angel_gadat032.py` 공통 코덱을 구현했다. 지원 형식과 전체 무수정
  왕복 결과는 다음과 같다.
  - RGBA32 1,287개
  - 8비트 색인+PS2 CLUT 445개
  - 4비트 색인+PS2 CLUT 43개
  - RGB24 55개
  - 640x480 분할 전송 RGB24 13개
  - 합계 TEX 1,843개 모두 렌더링 픽셀 해시가 일치했다.
- 색인형 텍스처는 무조건 팔레트를 새로 만들지 않고 원본 팔레트를 우선 보존해
  무수정 픽셀을 완전히 유지한다. 새 한글 픽셀은 가장 가까운 기존 UI 색에
  배정하며, 슬롯이 부족할 때만 새 팔레트 양자화를 사용한다.
- `galaxy_angel_patch_gadat032_images.py`를 일괄 재패커로 확장했다. 모든 변경
  PNG를 사전 검사한 뒤에만 ISO를 변경하며, GADAT032 로컬 PIDX, IDX.DAT 중앙
  미러, SGLRES의 동일 압축 런타임 복사본을 함께 갱신한다. 런타임 가용 공간은
  압축 스트림 뒤에 실제로 존재하는 0 패딩만 사용한다.
- 일본어 이미지 421개를 SGLRES와 전수 대조했다. 전투 런타임에 대응하는 것은
  101개 자산, 실제 복사본은 105개다. 대응표는
  `assets/image_extraction/japanese_images/runtime_audit.json`에 저장했다.
- 런타임 슬롯이 부족하면 원본 품질부터 시도한 뒤 256→128→64→32→16→8색
  순서로 처음 들어가는 최고 품질을 자동 선택한다. 사용자 시험용 `prbtn02`는
  기존 수동 시험의 16색/1,356바이트보다 나은 256색/1,825바이트로
  GADAT032와 SGLRES 양쪽에 들어갔고 별도 검증 도구를 통과했다.
- `build_patch.ps1`에 다음 필수 단계를 연결했다.
  - 전체 TEX 코덱 왕복 검사
  - SGLRES 런타임 대응 감사
  - 변경 PNG 자동 감지 및 동시 패치
  - 원시 TEX/렌더링 픽셀/중앙 IDX/런타임 복사본 검증
- 변경 이미지 0개인 상태로 정식 전체 빌드를 실행했다. 이미지 단계가 ISO를
  수정하지 않았고 기존 시나리오 210개, 선택지 339개, 외부 호출 337개,
  전투 블록 262개, 전투 런타임 복사본 795개 검증까지 모두 통과했다.
- 현재 정식 ISO: `build/Galaxy Angel (Korean).iso`
  - SHA-256: `6bd430ad9dd31559b7742d4f4ae24b1a07bdb10e9d77d22ddc5d6df49d790e8c`
  - 크기: `4,157,970,432`바이트
- 추가 도구:
  - `tools/galaxy_angel_gadat032.py`
  - `tools/galaxy_angel_patch_gadat032_images.py`
  - `tools/galaxy_angel_verify_gadat032_images.py`
  - `tools/galaxy_angel_verify_gadat032_codec.py`
  - `tools/galaxy_angel_audit_gadat032_runtime.py`

## 2026-08-24 일본어 UI 이미지 421개 전체 한글화

- 원본 작업 규칙을 수정했다.
  - `assets/image_extraction/japanese_images/png/`: 원본 PNG 421개, 읽기 전용
  - `assets/image_extraction/japanese_images/translated_png/`: 한글화 출력 421개
  - 파일명·실제 픽셀 크기·RGBA 모드·원본 알파를 유지하고, 비텍스트 UI는
    글자 마스크 밖에서 원본 픽셀을 보존했다.
- 파일명 정렬 순서로 전 범위를 완료했다.
  - 체크포인트 1: `help_00`~`pap069i` 72개
  - 체크포인트 2: `pfbtn01`~`pkttl15` 누계 153개
  - 체크포인트 3: `pmpop04`~`prbtn35f` 누계 280개
  - 체크포인트 4: `psbtn00`~`sentou` 누계 421개
- 완료 표기와 달리 빠져 있던 `help_00.png`도 복구했다. 이전에 완료된
  `help_01`~`help_05`는 사용자 변경을 유지했다.
- 대표 번역은 다음과 같다.
  - 미니게임: `뒤로`, `밀피유의 / 두근두근 쿠킹☆`, `란파의 / 살짝쿵☆쿵후`,
    `민트의 / 막과자 DE 퐁`, `포르테의 / 건·어택`,
    `바닐라의 / 기다려! 우기우기☆`, `두근두근☆ Flying Disk`
  - 장소: `엘시올 A~D블록`, `사령관실`, `브릿지`, `은하 전망 공원`,
    `티 라운지`, `우주 편의점`, 다섯 엔젤의 방, `격납고`, `트레이닝 룸`,
    `백색의 달・알현실`, `무도회장・귀빈석`, `우주항 로비` 등 51개
  - 전투/함선: `대기`, `이동`, `공격`, `필살기`, `호위`, `보급`, `수리`,
    `행동 설정`, `전체 명령`, `자동 색적`, `자동 반격`, `엘시올`,
    `럭키 스타`, `쿵후 파이터`, `바멜급 순양함`, `다크 엔젤` 등
  - 화면: `작전 정보/승리 조건/패배 조건`, `전투 결과`, `기체 정보`,
    `설정`, `불러오기`, `저장`, `처음부터`, `이어하기`, `앨범`, `미니게임`
  - 장 제목: `엔젤대 등장`, `강운의 천칭`, `점술 대소동`,
    `아가씨의 우울`, `달밤의 상담`, `치유의 힘`, `라스트 댄스`,
    `검은 절망`, `운명의 이정표`, `문 너머의 말`, `전해지는 마음`,
    `등 뒤의 신뢰`, `생명의 행방`, `최종결전`, `빛의 천사`
  - 이름은 기존 용어집을 재사용했다. 예: `택트・마이어스`,
    `밀피유・사쿠라바`, `레좀・메아・좀`, `카뮤・O・라프로이그`,
    `셰리・브리스톨`, `리셀바・키안티`.
- 421개 파일별 정확한 번역문은
  `assets/image_extraction/japanese_images/ui_translations.json`에 기록했다.
  색상 스와치 8개와 라틴 문자 `X` 타일 1개는 일본어가 없어 원본 픽셀을
  그대로 유지했고, 나머지 412개는 실제 픽셀이 변경됐다.
- 생성형 이미지 편집은 180x45 원본을 1888x832 검은 배경으로 재구성해
  원본 불변 조건을 위반했으므로 채택하지 않았다. 대신 글자 영역만 지우고
  Pretendard 기반 한글 글리프를 합성하는 결정론적 도구
  `tools/galaxy_angel_localize_ui_images.py`를 구현했다.
- `tools/galaxy_angel_verify_localized_ui.py`를 추가했다. 다음을 전수 검사한다.
  - 원본/출력 421개 파일명 1:1 대응
  - 실제 크기와 RGBA, 알파 및 투명 모서리 유지
  - `japanese_images/png`가 `GADAT032/png` 기준 원본과 픽셀 단위 동일
  - 투명 영역에 검은 불투명 배경이 새로 생기지 않음
  - SGLRES 공유 런타임 블록의 출력 픽셀 일치
  - 결과: 421개 통과, 변경 412개, 원본 유지 UI 9개
  - 보고서: `assets/image_extraction/japanese_images/localized_verification.json`
- `build_patch.ps1`의 이미지 입력을 `translated_png/`로 전환하고 위 421개
  검증을 필수 빌드 게이트로 연결했다. 원본 `png/`는 더 이상 빌드 입력이 아니다.
- `pibtn0/pubtn2`와 선택 상태 두 장은 같은 SGLRES 블록을 공유한다. 번역 픽셀을
  동일하게 통일하고, 패커가 공유 블록을 무조건 거부하던 규칙을 수정했다.
  이제 재구축 TEX와 압축 바이트가 완전히 같을 때만 공유를 허용하고 다르면
  빌드를 실패시킨다.
- 별도 ISO에 412개 변경 이미지를 전부 재패킹했다.
  - 패치된 이미지: 412개
  - SGLRES 런타임 복사본: 105개
  - 원본 품질: 409개
  - 256색 양자화: 설정 화살표 `poeff01~03` 3개
  - 검증: 원시 TEX, 렌더링 픽셀, 중앙 IDX, 런타임 복사본 모두 통과
  - 보고서: `build/ui_image_full_test_report.json`
  - 시험 ISO: `build/Galaxy Angel (Korean) ui-image-full-test.iso`
  - SHA-256: `b84d0d7efb3482be7e0ddebd54c428634bf2e3fc48dad87ada56b8e65a0b8c2a`
  - 크기: `4,157,970,432`바이트

## 2026-08-24 미니게임 로고 12개 전면 재작업

- 대상은 `mgl_00.png`~`mgl_05f.png` 12개뿐이며, 다른 이미지 409개는
  변경하지 않았다. 기존 `translated_png`의 12개 결과는 폐기하고 원본
  `japanese_images/png`를 기준으로 다시 만들었다.
- 각 원본을 built-in imagegen의 `text-localization` 편집 대상으로 개별
  처리했다. 생성 결과에서 확인한 둥근 글자 질량, 다중 외곽선, 캐릭터별
  팔레트와 하이라이트 구조를 참고하되, 생성 결과의 확대 크기·체크무늬 또는
  검은 배경·재생성된 캐릭터는 채택하지 않았다.
- 최종 합성은 투명 캔버스에서 굵은 손글씨 실루엣, 캐릭터색 외곽선,
  암청록 키라인, 그림자, 흰 에나멜 내부 그라데이션과 하이라이트를 별도
  이미지 층으로 재구성했다. normal/f는 동일한 형상을 쓰고 원본에서 추출한
  선택 팔레트만 다르게 적용했다. `mgl_05/05f`는 원본처럼 분홍→빨강→노랑
  →연두→파랑→보라의 다색 로고로 만들었다.
- 확정 문구:
  - `mgl_00/00f`: `밀피유의 두근두근 쿠킹☆`
  - `mgl_01/01f`: `란파의 살짝쿵☆쿵후`
  - `mgl_02/02f`: `민트의 막과자 DE 퐁`
  - `mgl_03/03f`: `포르테의 건·어택`
  - `mgl_04/04f`: `바닐라의 기다려! 우기우기☆`
  - `mgl_05/05f`: `두근두근☆ Flying Disk`
- 캐릭터 얼굴과 장식은 원본 우측 crop을 byte-for-byte로 복원했다. 최종
  12개 모두 원본 크기와 RGBA를 유지했고, 투명 배경, 불투명 검은 배경 없음,
  normal/f 알파 형상 일치, 캐릭터 영역 보존을 검증했다.
- 재현 도구: `tools/galaxy_angel_localize_ui_images.py`
- 전용 검증 도구: `tools/galaxy_angel_verify_mgl_logos.py`
- 검증 보고서:
  `assets/image_extraction/japanese_images/mgl_rework_verification.json`
- 원본/재작업 접촉시트:
  `assets/image_extraction/japanese_images/translated_review/mgl_original_vs_rework.png`
- 전체 421개 회귀 검증도 다시 통과했다: 변경 412개, 원본 유지 UI 9개,
  원본 421개가 기준 추출본과 픽셀 단위로 동일함을 확인했다.

## 2026-08-24 선택 전투 버튼 19개 및 설정 화살표 3개 색상·알파 수정

- 전체 일괄 처리는 중단한 상태에서 요청된 22개만 수정했다. 미니게임 로고
  재작업과 다른 이미지 일괄 번역은 재개하지 않았다.
- 선택 상태 전투 버튼 19개:
  `prbtn00f`, `prbtn01f`, `prbtn02f`, `prbtn03f`, `prbtn04f`, `prbtn05f`,
  `prbtn06f`, `prbtn07f`, `prbtn08f`, `prbtn09f`, `prbtn10f`, `prbtn11f`,
  `prbtn20f`, `prbtn21f`, `prbtn30f`, `prbtn31f`, `prbtn32f`, `prbtn34f`,
  `prbtn35f`.
  - 기존 문제는 `f` 원본의 분홍/빨강 선택 그라데이션 전체를 일반 파란
    배경 보간으로 덮은 것이었다.
  - 각 `f` 원본 자체의 2행 스캔라인 배경을 기준으로 일본어 글자 픽셀만
    복원하고 한국어를 다시 합성했다. 일반 상태 팔레트는 사용하지 않았다.
  - 원본 화살표, 좌우 테두리, 선택 색상, 그라데이션과 알파는 글자 마스크
    밖에서 픽셀 단위로 동일하다.
- 설정 화살표 `poeff01.png`~`poeff03.png`:
  - 확정 문구는 `게임 설정`, `비디오 설정`, `사운드 설정`이다.
  - 원본 일본어는 불투명한 검정 글자가 아니라 파란 배경의 RGB/알파를
    단계적으로 깎은 투명 컷아웃 글자였다. 원본별 글자 픽셀의 RGB 비율과
    100단계 이상의 알파 응답을 측정해 한국어 안티앨리어싱 마스크에
    이식했다. 글자 중심은 원본처럼 완전 투명 RGBA `(0,0,0,0)`이고 가장자리는
    원본 팔레트에서 측정한 반투명 단계다.
- 최종 출력: `assets/image_extraction/japanese_images/translated_png/`
- 재현·재발 방지:
  - `tools/galaxy_angel_localize_ui_images.py`에 `f` 전용 선택 배경 복원과
    `poeff` 원본 RGBA 응답 전송 경로를 추가했다.
  - `tools/galaxy_angel_verify_focus_ui.py`를 추가했다.
- 합동 검증 결과: 22개 모두 실제 원본 크기, RGBA, 알파 구조 유지 및
  글자 마스크 밖 변경 픽셀 0개.
  - 보고서: `assets/image_extraction/japanese_images/focus_ui_verification.json`
  - 전체 접촉시트:
    `assets/image_extraction/japanese_images/translated_review/focus_ui_original_vs_fixed.png`
  - 전투 버튼 접촉시트:
    `assets/image_extraction/japanese_images/translated_review/prbtn_focus_original_vs_fixed.png`
  - 설정 화살표 접촉시트:
    `assets/image_extraction/japanese_images/translated_review/poeff_focus_original_vs_fixed.png`
- 수정 후 전체 421개 회귀 검증도 다시 통과했다: 변경 412개, 원본 유지 UI
  9개, 원본 421개가 기준 추출본과 픽셀 단위로 동일함을 확인했다.

## 2026-08-24 `pubase`·`ppwin0`·`pmpop15` 원문 좌표 배치 수정

- 전체 일괄 처리는 계속 중단하고 다음 3개만 원본에서 다시 합성했다.
- `pubase.png`
  - `기체 정보`부터 `유닛 선택`까지 20개 원문 바운딩 박스를 측정했다.
  - 번역문을 각 원문 `left/top`에서 시작하도록 변경하고, 원문 박스 폭을
    넘지 않게 해 오른쪽 예약 공간을 침범하지 않도록 했다.
  - 예: `기체 정보 (70,19)`, `최고 속도 (261,152)`,
    `사정거리 (440,153)`, `유닛 선택 (438,447)`.
  - 원문 `射程`의 좁은 폭에 맞추느라 10px 높이로 축소됐던 `사정거리`는
    시작점 `(440,153)`을 유지하면서 `장갑 성능`과 동일한 15px 높이로
    다시 합성했다. 최종 bbox는 `(440,153,490,168)`이다.
- `ppwin0.png`
  - 제목·열 제목·기체 행 15개의 원문 `left/top/픽셀 높이`를 측정해
    번역문에 그대로 적용했다. 모든 항목의 번역 픽셀 높이가 원문과 같다.
  - 폭이 좁아 원문이 2행이던 `累積撃墜数`는 기존 문구를 유지해
    `누적 / 격추` 2행으로 배치했다. 원문과 번역문 모두
    bbox `(465,45,*,80)`, 높이 35px다.
  - 원문 `計`의 15px 폭 때문에 작게 압축됐던 `합계`는 원문 시작점
    `(30,341)`과 높이 17px를 유지하되, 바로 위 `기타`와 동일한
    30×17px 글자 크기로 다시 합성했다. 최종 bbox는 `(30,341,60,358)`이다.
- `pmpop15.png`
  - 원문 하단 라벨 bbox는 `(9,100,147,123)`, 중심은
    `(78.0,111.5)`로 측정됐다.
  - `밀피유의 방`을 캔버스 중심이 아닌 동일한 하단 라벨 중심
    `(78.0,111.5)`에 배치했다.
- 세 파일 모두 원본 크기·RGBA·알파를 유지했고, 원문/번역 글자 마스크
  밖의 변경 픽셀은 0개다.
- 재현 코드: `tools/galaxy_angel_localize_ui_images.py`
- 좌표 전용 검증: `tools/galaxy_angel_verify_layout_focus.py`
- 수치 보고서:
  `assets/image_extraction/japanese_images/layout_focus_verification.json`
- 원본 빨간 bbox/수정본 초록 bbox 비교 이미지:
  `assets/image_extraction/japanese_images/translated_review/layout_focus_bbox_original_vs_fixed.png`
- 수정 후 전체 421개 회귀 검증도 통과했다: 변경 412개, 원본 유지 UI 9개,
  원본 421개가 기준 추출본과 픽셀 단위로 동일하다.

## 2026-08-24 이미지 수정 반영 정식 ISO 재빌드

- `assets/image_extraction/japanese_images/translated_png/`의 현재 421개를
  입력으로 정식 `build_patch.ps1` 전체 빌드를 완료했다.
- 사용자가 의도적으로 완전 투명화한 다음 `pxttl` 11개는 복구하거나
  재생성하지 않고 그대로 TEX로 압축·삽입했다:
  `pxttl03`, `pxttl04`, `pxttl05`, `pxttl12`, `pxttl13`, `pxttl22`,
  `pxttl33`, `pxttl43`, `pxttl52`, `pxttl73`, `pxttl74`.
- 이미지 패치 결과는 변경 412개, SLGRES 런타임 복사본 105개이며 이미지
  보고서와 완성 ISO 재검증을 모두 통과했다.
- 시나리오 210개, 외부 호출/선택지 구조, 런타임 인덱스, 전투 블록 262개와
  런타임 복사본 795개 검증도 모두 통과했다.
- 완성 ISO: `build/Galaxy Angel (Korean).iso`
- 크기: `4,157,970,432`바이트
- SHA-256: `a203e0220377c2f1f012fbde066b7e0303569105490de008a12e2234d454ef44`

## 2026-08-24 메인 메뉴·설정 MISC 런타임 이미지 및 전투 조건 누락 수정

- 메인 메뉴 `ptbtn00` 계열과 설정 화면 `pobase`·`pobtn`·`poeff`가
  GADAT032에서는 번역됐지만 실제 화면은 `MISC.DAT` 내부 복사본을 읽고 있었다.
  기존 이미지 패처는 `SLGRES.DAT`만 검사해 이 복사본들을 놓쳤다.
- `galaxy_angel_audit_gadat032_runtime.py`와 `build_patch.ps1`을 확장해
  `SLGRES`와 `MISC`를 모두 감사·패치하도록 변경했다.
  - 전체 421개 중 런타임 자산 268개, 복사본 272개를 발견했다.
  - 변경 이미지 412개와 연결된 복사본 264개를 실제 ISO에 패치했다:
    `MISC` 159개, `SLGRES` 105개.
  - `ptbtn00~04`, `ptbtn10~12`의 normal/f, 설정 화면 `pobase`,
    `pobtn01~06/15`, `poeff01/03/04/06/07/08`의 MISC 복사본이 모두
    GADAT032의 번역 TEX와 바이트 단위로 같은 해제 데이터임을 검증했다.
- 전투 작전 정보의 `・敵艦隊の全滅`은 원본 GADAT002에 12번 있지만 번역
  발생 위치 목록에는 5번만 들어 있어 7번이 일본어로 남았다.
  - `galaxy_angel_build_battle.py`가 GADAT002 전체에서 완전한 필드 값을
    재대조하고 누락 위치를 자동 추가하도록 수정했다.
  - 기존 전투 대사 바이트 범위와 겹치는 후보는 자동 제외한다.
  - SLGINIT/SLGRES의 모든 FSTS 스트림도 해제 원문 해시로 재대조해 동일한
    런타임 복사본을 빠짐없이 연결한다.
  - 적용된 remaining 단위는 1,954개에서 1,961개로 증가했다.
  - 완성 ISO 검사 결과 해당 일본어 원문은 GADAT002와 런타임 모두 0개,
    `・적 함대의 전멸`은 각각 12개다.
- 사용자가 완전 투명화한 `pxttl` 11개는 그대로 유지했다.
- 완성 ISO: `build/Galaxy Angel (Korean).iso`
- 크기: `4,157,970,432`바이트
- SHA-256: `ef2b32f170ab379fc66d88f3c7e2f207d5d5c5b48f0dfab0fab83f168c4fe7b3`

## 2026-08-24 전체 런타임 이미지 컨테이너 반영 최종본

- 메인 메뉴·설정 외에도 GADAT032 이미지의 실제 런타임 복사본이 있는
  `SLGRES`, `MISC`, `SLGINIT`, `ADV`, `ALBUM` 5개 컨테이너를 모두
  감사하고 패치하도록 이미지 빌드 경로를 확장했다.
- 번역 이미지 412개를 실제 런타임 복사본 1,355개에 반영했다.
  - `ADV`: 81개
  - `ALBUM`: 32개
  - `MISC`: 159개
  - `SLGINIT`: 978개
  - `SLGRES`: 105개
- 메인 화면의 `ptbtn00` 계열과 설정 화면 이미지가 있는 `MISC` 복사본을
  포함해, 412개 이미지와 1,355개 런타임 복사본의 TEX 해제 결과를
  완성 ISO에서 재검증했다. 팔레트 제한 때문에 필요한 32개 텍스처의
  양자화 결과도 검증을 통과했다.
- 사용자가 의도적으로 완전 투명화한 `pxttl03`, `pxttl04`, `pxttl05`,
  `pxttl12`, `pxttl13`, `pxttl22`, `pxttl33`, `pxttl43`, `pxttl52`,
  `pxttl73`, `pxttl74`는 각각 ALBUM 런타임 복사본 1개에 적용됐다.
  11개 모두 동일한 완전 투명 TEX이므로 해시 기반 사후 감사에서는 동일한
  ALBUM 슬롯이 파일별 후보로 중복 집계되지만, 패치 보고서상 실제 적용은
  파일별 1개씩이다.
- 전투 작전 정보에서 빠졌던 `・敵艦隊の全滅` 7개를 자동 발견해 기존
  5개와 함께 모두 `・적 함대의 전멸`로 반영했다. 최종 전투 검증 결과:
  전투 블록 262개, 리다이렉트 45개, IDX 레코드 426개, 런타임 복사본
  795개(`SLGINIT` 794개, `SLGRES` 1개) 모두 정상이다.
- 시나리오 구조, 선택지/외부 호출, 전체 런타임 인덱스 검증도 빌드 과정에서
  모두 통과했다.
- 현재 최종 ISO: `build/Galaxy Angel (Korean).iso`
- 크기: `4,157,970,432`바이트
- SHA-256: `fc989f139a05962b9ed4e8195dab53b7b2bc8b512a3dba8d257bcb6a216c37fb`
- 위의 `a203...`, `ef2b...` 해시는 중간 빌드이며 이 최종본으로 대체됐다.

## 2026-08-26 표시용 물결표 전각 통일

- 게임 화면에 표시되는 한국어 번역에서 물결표는 ASCII 반각 `~`를 사용하지 않는다.
  원작 일본어 폰트/렌더러가 사용하는 전각 `～`(U+FF5E)만 사용한다.
- 이 규칙은 GADAT001 일반 대사, GADAT002 전투 대사, 선택지, 메인 ELF의
  미니게임 문자열에 공통 적용한다. 문서에서 범위를 설명하는 `A~B` 표기는
  화면 문자열이 아니므로 예외다.
- `galaxy_angel_translation.py`에 표시용 문장부호 정규화를 추가해 활성 시나리오
  번역의 반각 `~`를 빌드 시 전각 `～`로 바꾼 뒤 줄폭 검증과 인코딩을 수행한다.
- `galaxy_angel_build_battle.py`도 전투 문자열 인코딩 전에 같은 정규화를 적용한다.
- `galaxy_angel_minigame_elf.py`는 기존 Gemma 캐시를 읽을 때와 새 번역/축약 결과를
  받을 때 모두 `~`→`～`를 정규화하며, Gemma 프롬프트에도 전각 사용 규칙을 명시한다.
  따라서 이전 캐시에 반각 물결표가 남아 있어도 다음 빌드에서 자동 교정된다.
- 미니게임 ELF 캐시 644개를 다시 정규화한 결과 표시 문자열의 반각 `~`는 0개,
  전각 `～`가 포함된 문자열은 32개다. 전투 262블록과 런타임 복사본 795개도
  새 인코더 기준 검증을 통과했다.
- 기존 `Galaxy Angel (Korean).iso`가 실행 중인 프로그램에 잠겨 있어 검증용 완성본은
  `build/Galaxy Angel (Korean) Wave.iso`로 생성했다. 이미지 473개/런타임 복사본
  1,421개, 시나리오 인덱스 1,166개, cross-section 점프 869개까지 전수 검증했다.
- 검증용 ISO 크기: `4,156,518,400`바이트
- 검증용 ISO SHA-256: `4cab5cbd5637a344731d3064ebdef9292646832398cab5c796bba5ad2f01c435`

## 2026-08-26 밀피유 샌드위치 미니게임 레시피 이미지 번역

- 샌드위치 미니게임의 카드 상단 `レシピ`는 GADAT032 UI가 아니라 `MINI.DAT`의
  `mini/mini00/resipi.agi` 4bpp AGI 이미지에서 그려진다. 카드 번호는 별도
  `resip_no.agi`를 사용하므로 `resipi.agi`만 `레시피`로 교체하면 레시피 1~10에
  공통 적용된다.
- 원본 `resipi.agi`는 64x32이며 실제 글자 점유 영역은 `x=0..49, y=0..20`이다.
  Pretendard Bold 18px와 2px 외곽선을 사용해 `레시피`를 같은 50x21 영역에 맞춰
  다시 그린다. 원본 AGI 팔레트, 이미지 크기, 카드에서의 배치 위치는 그대로 유지한다.
- 새 패처 `galaxy_angel_patch_minigame_recipe.py`는 MINI.DAT의 전체 PIDX 디렉터리
  트리를 이름까지 해석해 정확히 `mini/mini00/resipi.agi`만 수정한다. 새 AGI는 원본
  16색 팔레트로 재양자화하고 원래 물리 슬롯에서 재압축하며, MINI.DAT 로컬 PIDX와
  중앙 `IDX.DAT`의 압축 크기 미러도 함께 갱신하고 다시 해제해 바이트 검증한다.
- `build_patch.ps1`의 기본 빌드 체인에 이 패치를 포함시켜 이후 ISO 재빌드에서도
  자동으로 반영한다.
- 전체 빌드를 다시 실행해 최종 ISO까지 검증했다. `resipi.agi`는 원본 물리 위치
  `0xBE000`, raw 1,136바이트를 유지하며 압축 크기만 `666 -> 561`바이트로 줄었다.
  MINI.DAT 로컬 PIDX `(778240, 1136, 561)`과 중앙 IDX.DAT file id 153 미러가
  일치하고, 최종 ISO에서 다시 해제한 AGI가 기대 렌더링과 바이트 단위로 일치한다.
- 최종 ISO: `build/Galaxy Angel (Korean).iso`
  - 크기: `4,156,518,400`바이트
  - SHA-256: `3739a0bd460bdefa2c5567761141b35747da61266b33c64e40b400eb8f6e162d`

## 2026-08-27 레시피 이미지 적용 재검증

- 사용 중인 최종 ISO에서 `MINI.DAT/mini/mini00/resipi.agi`를 직접 다시 해제해
  확인했으며, 64x32 픽셀은 `레시피` 렌더링과 바이트 단위로 일치한다.
- 일반 MINI 이미지 패킹이 이후에 대상 이미지를 덮어쓸 가능성까지 차단하기 위해
  `build_patch.ps1`의 레시피 패치 단계를 MINI 이미지 전체 패킹 뒤로 이동했다.
- 레시피 패처는 이미 동일한 압축 크기와 중앙 IDX 미러가 적용된 ISO에도 안전하게
  재실행할 수 있도록 멱등 처리했다. 현재 로컬 PIDX는 `(778240, 1136, 561)`,
  중앙 IDX file id는 `153`이며 재해제 검증을 통과했다.
- 최종 ISO: `build/Galaxy Angel (Korean).iso`
  - 크기: `4,156,518,400`바이트
  - SHA-256: `1e27ddfe40c31a423c38990de999db0bb40326c167b71d0f7a9046a97c396ed8`
- PCSX2 저장 상태에는 이전의 일본어 텍스처가 RAM/VRAM과 함께 저장될 수 있다.
  검수 시 저장 상태 불러오기가 아니라 최신 ISO를 새로 부팅한 뒤 미니게임에
  진입하거나, 최소한 미니게임을 완전히 나갔다가 다시 진입해 텍스처를 재로딩한다.

## 2026-08-27 MINI.DAT 이미지 최종 ISO 전수 검증

- `galaxy_angel_verify_minigame_iso.py`를 추가해 번역 PNG가 있다는 사실이나 패치
  보고서만 확인하지 않고, 완성 ISO의 `MINI.DAT`에서 모든 대상 이미지를 다시
  해제하여 픽셀 해시를 직접 비교한다.
- 번역 이미지 185종이 쓰이는 61개 리소스의 272개 위치와 별도 레시피 이미지
  1개를 합친 총 273개 위치를 전수 검사했으며 불일치는 0건이다.
- 일본어 후보 자동 탐지에서 제외된 28종도 접촉시트로 다시 검토했다. 숫자·순위
  (`4th.`, `5th.`, `pts.`), 영문 UI(`Character Select`, `VS.COM`), 배경·캐릭터·아이콘
  이미지뿐이며 번역이 필요한 일본어 이미지는 없었다.
- `build_patch.ps1`에 이 최종 ISO 검증을 기본 단계로 추가했다. 이후에는 일반 MINI
  이미지 272곳과 레시피가 하나라도 누락되거나 덮어써지면 빌드가 실패한다.
- 검증 보고서: `build/mini_image_iso_verification.json`
  - `verified_occurrences`: 273
  - `verified_resources`: 61
  - `recipe_verified`: true
  - `mismatch_count`: 0

## 2026-08-28 MINI.DAT 실제 런타임 이미지 풀 패치

- 실기 검수에서 이름이 붙은 PIDX 이미지가 번역되어 있어도 게임 화면은 일본어인
  사실을 확인했다. 기존 검증은 PIDX 원본 슬롯만 검사했기 때문에 실제 로더 경로를
  검증하지 못했다.
- 일본 원본 ISO의 압축 블록을 기준으로 현재 `MINI.DAT` 전체를 다시 검색한 결과,
  번역 대상 61개 리소스 모두에 이름 없는 16바이트 정렬 런타임 복사본이 하나씩
  남아 있었다. `resipi.agi`도 PIDX 위치 `0xBE000`과 별개로 런타임 위치
  `0xB39320`에 일본어 원본 블록이 남아 있었다.
- `galaxy_angel_patch_minigame_runtime_images.py`를 추가해 일반 리소스 61개와 레시피
  1개, 총 62개의 런타임 블록을 시작 위치를 움직이지 않고 번역 블록으로 교체했다.
- 번역 압축 데이터가 원래 16바이트 슬롯보다 큰 독립 AGI 6개는 원본 16색 팔레트의
  시각적으로 가장 가까운 안티앨리어싱 색 단계 한 쌍만 병합했다. 문구·글자 위치·
  투명도·캔버스는 유지되며, `mini_runtime_palette_fit_contact_sheet.png`에서 PIDX
  번역본과 런타임용 결과를 확대 대조했다.
- 패치 후 62개 일본어 원본 압축 블록의 잔존 수는 0개다. PIDX 번역 이미지 273곳,
  GADAT032 이미지 473개와 런타임 복사본 1,421개, 시나리오/전투/런타임 인덱스
  검증도 모두 다시 통과했다.
- 이후 전체 빌드에서도 재발하지 않도록 MINI 런타임 풀 패치를 모든 다른 ISO 패치와
  검증이 끝난 뒤 마지막 단계로 배치했다.
- 보고서: `build/mini_runtime_image_patch_report.json`
  - `patched_resources`: 62
  - `palette_fitted_resources`: 6
  - `remaining_japanese_blocks`: 0
- 최종 ISO: `build/Galaxy Angel (Korean).iso`
  - 크기: `4,156,518,400`바이트
  - SHA-256: `747838130fa6c572bab48b955b581cbf892ffe38aefd2073d903a5779c8314b1`

## 2026-08-28 MINI.DAT FSTS 런타임 길이 테이블 수정

- 실제 게임에서 레시피의 일본어 `レシピ`는 사라졌지만 한글 `레시피`도 표시되지
  않는 현상을 확인했다. 완성 ISO의 named/runtime `resipi.agi`를 직접 해제하면
  두 위치 모두 64x32 한글 픽셀이 정상이며 raw SHA도 동일했으므로 이미지 자체가
  비어 있는 문제는 아니었다.
- `MINI.DAT`의 런타임 풀 앞에 별도 `FSTS` 인덱스가 존재한다. 구조는 32바이트
  헤더 뒤 16바이트 레코드 `resource_id / relative_offset / raw_size /
  compressed_size`이며, 레시피는 FSTS base `0xAB8800`, record `0xAB8F40`,
  resource id `0xAC8`, relative offset `0x80B20`이다. base와 relative offset을
  더하면 실제 런타임 블록 `0xB39320`과 정확히 일치한다.
- 기존 런타임 패처는 블록 시작 위치와 16바이트 슬롯만 유지하고 한글 스트림을
  `666 -> 561`바이트로 줄였지만 FSTS의 `compressed_size=666`을 갱신하지 않았다.
  게임은 이 오래된 길이로 trailing padding까지 리소스로 읽어 텍스처 로드가
  실패했고, 결과적으로 카드 상단 글자가 빈 상태로 표시됐다.
- `galaxy_angel_patch_minigame_runtime_images.py`를 수정해 62개 런타임 이미지마다
  실제 FSTS 레코드를 1:1로 찾아 번역 스트림 길이까지 함께 갱신한다. 이미 블록이
  패치된 ISO는 기존 보고서의 raw SHA/실제 consumed 길이를 대조한 뒤 FSTS만 빠르게
  수리하는 `--repair-fsts-only` 모드도 추가했다.
- 현재 ISO의 62개 FSTS 레코드를 모두 수리했다. 레시피는 `(raw=1136,
  compressed=561)`로 바뀌었고 실제 decompressor consumed도 561바이트다. 62개 전체에서
  `FSTS raw/compressed == 실제 decoded/consumed`가 일치하고 raw SHA 불일치는 0건이다.
- `galaxy_angel_verify_minigame_iso.py`를 v2로 확장해 named MINI 273곳뿐 아니라
  런타임 FSTS 62개도 최종 ISO에서 다시 읽어 검증한다. `build_patch.ps1` 마지막에
  이 검증을 추가해 이후 FSTS 길이가 stale이면 빌드를 실패시킨다.
- 최종 검증: `unique=185`, named occurrence `273`, runtime FSTS `62/62`,
  mismatch `0`.
- 최종 ISO: `build/Galaxy Angel (Korean).iso`
  - 크기: `4,156,518,400`바이트
  - SHA-256: `57a6e2d6fdbb9d255c017833d346fce56640d115dd1a718295fc3acce09b9dac`

## 2026-08-28 레시피 런타임 LZ 스트림 호환성 재검증

- FSTS `compressed_size`를 실제 스트림 길이와 맞춘 뒤에도 실기 화면에서 카드 상단
  한글이 표시되지 않았다. 따라서 위 FSTS 길이 불일치는 실제 버그였지만, 레시피
  미표시의 단독 원인이라고 단정할 수 없음을 확인했다.
- named/runtime `resipi.agi`의 해제 raw와 64x32 픽셀은 계속 동일했고 헤더 0x30바이트도
  일본 원본과 동일했다. 다음 차이로 남은 것은 새 압축 스트림 자체이므로 레시피에 한해
  `ikusa_lz.compress_optimal()` 대신 프로젝트에서 오래 사용해 온 일반
  `ikusa_lz.compress()` 경로로 다시 압축했다.
- 같은 `레시피` raw의 압축 크기는 `561 -> 567`바이트가 됐다. named PIDX,
  중앙 IDX.DAT, FSTS 런타임 복사본을 모두 같은 567바이트 스트림으로 동기화했다.
  런타임 물리 슬롯은 672바이트이므로 시작 위치와 다음 리소스 위치는 변하지 않는다.
- 현재 확인값:
  - named offset `0xBE000`, raw `1136`, compressed/consumed `567/567`
  - runtime offset `0xB39320`, FSTS record `0xAB8F40`, compressed/consumed `567/567`
  - named/runtime raw SHA-256 `fa2a5424b3ab5681b264a230d256ae775834e435c5808b43bac5f1cc0e49d961`
  - MINI named 검증 273곳, 레시피 runtime/FSTS 검증 1/1, mismatch 0
- `galaxy_angel_patch_minigame_runtime_images.py`에는 빠른 실기 반복용 `--recipe-only`
  모드를 추가했다. 이 변경은 아직 실기 표시 재확인 전이며, 표시 성공으로 기록하지 않는다.
- 실기 재확인 대상 ISO: `build/Galaxy Angel (Korean).iso`
  - 크기: `4,156,518,400`바이트
  - SHA-256: `8696cd934d25fc99c48d898f3463b21ba194df5288b856b4a97fa660a08e97b6`

## 2026-09-04 frozen 이미지 기준 최종 배포 v1.0

- 현재 승인 이미지 전체를 `build/frozen_images_20260904_1630`으로 고정하고 해당
  snapshot만 최종 빌드 입력으로 사용했다. UI 482장 + MINI 275장, 총 757장의
  SHA-256을 작업 종료 시 다시 검사했으며 live 원본 mismatch 0, frozen copy mismatch 0이다.
- 최종 빌드 디렉터리는 `build/final_frozen_20260904_1630`이다. 일반 MINI 번역
  268 occurrence와 직접 추가 8 occurrence를 완성 ISO에서 다시 해제했고, runtime
  FSTS 63/63도 raw SHA/decoded size/compressed consumed까지 모두 일치하며 mismatch 0이다.
- `mini/mini05/for_kl.agi` 등 세 리소스는 canonical translated block이 원래 runtime
  슬롯보다 컸다. 픽셀/팔레트 색을 병합하지 않고 같은 MINI05 FSTS 테이블 안의
  기존 슬롯을 3쌍 교환해 해결했다. 재배치된 FSTS record는 6개이며 palette merge는 0개다.
  MINI05 FSTS table `0x115D800`의 214개 record는 최종 ISO에서 offset 214개 모두
  유일하고 stream overlap 0, strict LZ raw/size mismatch 0을 확인했다.
- 최종 통합 재검증:
  - GADAT032 이미지 473개 / runtime copy 1,417개 / quantized TEX 72개 PASS
  - 시나리오 210개 / indexed target 1,166개 / cross jump 869개 PASS
  - stale runtime scenario index 0
  - 전투 262개 / runtime copy 795개 PASS
  - localized UI 482개, GADAT032 TEX codec round-trip 1,843개 PASS
  - 일본어 이미지 runtime audit 431 assets / 1,478 runtime copies 완료
- 최종 ISO: `build/final_frozen_20260904_1630/Galaxy Angel (Korean).iso`
  - 크기: `4,156,518,400`바이트
  - MD5: `0525bada52e223f12ff493462df2eeed`
  - SHA-1: `094133b564504c517343a3a297d1f80a9b52f859`
  - SHA-256: `ab362ed34e4acc8c0206727de534de7a91cb1b9839c416a21e9bbeebf84fda15`
- 배포 XDelta: `release/galaxy_angel_ps2_kr_v1.0.xdelta`
  - 크기: `18,011,520`바이트
  - SHA-256: `a9078fd3e35109502270b0c20dd534983fc2033f6a9d9e28ffe538ceabba203f`
- XDelta를 지원 원본 ISO에 실제 역적용해 `build/release_verify_v1.0.iso`를 만들고
  최종 ISO와 크기/MD5/SHA-1/SHA-256 전부 일치하는 것을 확인했다.
- 배포 메타데이터는 `release/SHA256SUMS.txt`, `release/release.json`,
  `release/README.txt`에 확정했다.
